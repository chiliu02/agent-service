{
  "openapi": "3.1.0",
  "info": {
    "title": "agent-service",
    "summary": "HTTP access to Gemini CLI headless (gemini-python).",
    "version": "0.21.0"
  },
  "paths": {
    "/healthz": {
      "get": {
        "tags": [
          "meta"
        ],
        "summary": "Healthz",
        "description": "Liveness, plus a LIVE report on the credential.\n\nCredentials that disappear after boot do not stop a running service, so\nthis is read now rather than remembered from startup.",
        "operationId": "healthz_healthz_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Health"
                }
              }
            }
          }
        }
      }
    },
    "/v1/deployment": {
      "get": {
        "tags": [
          "meta"
        ],
        "summary": "Deployment",
        "description": "Every difference a client must act on (AS-32).\n\nRead this instead of branching on the image tag. If you find yourself\ndiffing two documents, that is a defect on this side.",
        "operationId": "deployment_v1_deployment_get",
        "responses": {
          "200": {
            "description": "This build's capabilities. **The example is real** -- every field in it except the deployment-dependent ones (`workspace_dir`, `limits`, `max_sessions`, `require_credentials`, `require_mounts`, `auth_required`, `allow_mcp_servers`, `default_model`, `reference_dirs`) is what a live instance returns, and a test pins that so it cannot drift. Those exceptions show this build's defaults; read the endpoint for an instance's own. **Three version strings read `x.y.z` and are not defaults but placeholders** -- `impl.version`, `sdk.version` and `sdk_version` move whenever this build does, and this document is frozen at a release, so a real value here would stop matching the service it describes. Read them from the endpoint, or from the image before it runs.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Deployment"
                },
                "example": {
                  "service": {
                    "spec": {
                      "document_version": "0.21.0"
                    },
                    "impl": {
                      "name": "gemini-python",
                      "version": "x.y.z"
                    },
                    "sdk": {
                      "name": "gemini-cli",
                      "version": "x.y.z"
                    },
                    "sdk_version": "x.y.z"
                  },
                  "config": {
                    "workspace_dir": "/workspace",
                    "reference_dirs": [],
                    "default_model": "auto",
                    "max_sessions": 8,
                    "require_credentials": true,
                    "require_mounts": false,
                    "auth_required": false,
                    "allow_mcp_servers": true,
                    "credential_sources": [
                      "GEMINI_API_KEY"
                    ],
                    "provider_selectors": [
                      "GOOGLE_GENAI_USE_VERTEXAI",
                      "GOOGLE_GENAI_USE_GCA"
                    ]
                  },
                  "accepts": {
                    "permission_modes": [
                      {
                        "id": "default",
                        "name": "Default",
                        "description": "Prompts for approval, which in headless means the agent decides for itself. NOT a boundary: across nine trials of one prompt it wrote once, declined three times and failed to terminate five times. The generated tool policy is what actually confines a turn."
                      },
                      {
                        "id": "auto_edit",
                        "name": "Auto edit",
                        "description": "Auto-approves edit tools. Deterministic, unlike `default`."
                      },
                      {
                        "id": "yolo",
                        "name": "YOLO",
                        "description": "Auto-approves every tool. **Also switches OFF the agent's built-in shell redirection guard**, so this build always denies redirection with an explicit policy rule instead."
                      },
                      {
                        "id": "plan",
                        "name": "Plan",
                        "description": "Read-only mode, as the agent defines it. Unmeasured here."
                      }
                    ],
                    "effort_levels": [],
                    "setting_sources": [],
                    "default_allowed_tools": [
                      "read_file",
                      "write_file",
                      "list_directory",
                      "glob",
                      "grep_search"
                    ],
                    "always_disallowed_tools": [
                      "run_shell_command"
                    ],
                    "unsupported_options": [
                      {
                        "field": "effort",
                        "types": null,
                        "values": null
                      },
                      {
                        "field": "setting_sources",
                        "types": null,
                        "values": null
                      },
                      {
                        "field": "max_budget_usd",
                        "types": null,
                        "values": null
                      },
                      {
                        "field": "max_turns",
                        "types": null,
                        "values": null
                      },
                      {
                        "field": "strict_mcp_config",
                        "types": null,
                        "values": [
                          false
                        ]
                      },
                      {
                        "field": "system_prompt",
                        "types": [
                          "object"
                        ],
                        "values": null
                      }
                    ],
                    "strict_mcp_config": true,
                    "allow_supplied_sdk_session_id": false,
                    "mcp": {
                      "transports": [
                        "stdio",
                        "sse",
                        "http"
                      ],
                      "http_headers": "any",
                      "server_name_pattern": "^[^_]+$"
                    },
                    "limits": {}
                  },
                  "behaviour": {
                    "limits": {
                      "turn_timeout_s": 600.0,
                      "max_sessions": 8.0,
                      "session_idle_ttl_s": 1800.0
                    },
                    "mcp_tool_call": {
                      "request_timeout_s": 60,
                      "idle_timeout_s": null,
                      "total_timeout_s": 600,
                      "progress_resets_idle": false
                    },
                    "sandbox": {
                      "network_access": true,
                      "confines_writes_to_workspace": false
                    },
                    "permission_enforcement": "none",
                    "model_usage_scope": "per_turn",
                    "reports_cost_usd": false,
                    "usage_counts_tool_calls": false,
                    "turn_token_overhead": 7000.0,
                    "llm_correlation": {
                      "header": null,
                      "measured": true
                    },
                    "sdk_session_id_scope": "turn",
                    "query_reports_sdk_session_id": true,
                    "query_consumes_a_session_slot": false
                  }
                }
              }
            }
          }
        }
      }
    },
    "/v1/schemas/run-options": {
      "get": {
        "tags": [
          "meta"
        ],
        "summary": "Run options schema",
        "description": "**`deployment.accepts`, rendered as JSON Schema 2020-12** -- the published `RunOptions` narrowed by what THIS deployment accepts, so a client can validate a request instead of branching on a capability payload.\n\nOne fact in two shapes, and a conformance clause asserts they agree. It is deployment-specific by nature: two containers of one image answer differently, which is why it is served rather than frozen into a document.\n\nSelf-contained -- every `$ref` resolves inside `$defs`, so a validator needs no network.",
        "operationId": "run_options_schema_v1_schemas_run_options_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {}
              },
              "application/schema+json": {}
            }
          }
        }
      }
    },
    "/v1/sessions": {
      "get": {
        "tags": [
          "sessions"
        ],
        "summary": "List Sessions",
        "description": "**Answered from our own store, never from the agent** (GP-14).\n\n`--list-sessions` hides sessions that are still resumable and lists ones\nabout to be deleted, so it cannot answer this route truthfully.",
        "operationId": "list_sessions_v1_sessions_get",
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SessionList"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "sessions"
        ],
        "summary": "Create Session",
        "description": "Open a session. **A supplied `sdk_session_id` is refused** (GP-34).\n\nNeither the CLI's `--session-id` nor anything else survives the durable\nresume path, so adopting a caller's id and then returning a different one\nwould break the single guarantee supplying it provides, invisibly.",
        "operationId": "create_session_v1_sessions_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SessionCreate"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SessionRecord"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "429": {
            "description": "Too Many Requests",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "503": {
            "description": "Service Unavailable",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "504": {
            "description": "Gateway Timeout",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ValidationProblem"
                }
              }
            }
          }
        }
      }
    },
    "/v1/sessions/{sid}": {
      "get": {
        "tags": [
          "sessions"
        ],
        "summary": "Get Session",
        "operationId": "get_session_v1_sessions__sid__get",
        "parameters": [
          {
            "name": "sid",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Sid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SessionRecord"
                }
              }
            }
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            },
            "description": "Not Found"
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ValidationProblem"
                }
              }
            }
          }
        }
      },
      "delete": {
        "tags": [
          "sessions"
        ],
        "summary": "Delete Session",
        "description": "Close a session. **The transcript survives**, so a resume still works.",
        "operationId": "delete_session_v1_sessions__sid__delete",
        "parameters": [
          {
            "name": "sid",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Sid"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            },
            "description": "Not Found"
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            },
            "description": "Internal Server Error"
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ValidationProblem"
                }
              }
            }
          }
        }
      },
      "patch": {
        "tags": [
          "sessions"
        ],
        "summary": "Update Session",
        "description": "Change the model or the permission mode mid-session.\n\n**Omitted fields are never forwarded.** Both are passed to the agent per\nturn — `-m` and `--approval-mode` — so writing a null would mean sending\nthe flag with nothing behind it, which is not the same as leaving it\nalone.\n\n**The read-back is what a caller gets**, so it can see what was actually\ntaken rather than what it asked for.",
        "operationId": "update_session_v1_sessions__sid__patch",
        "parameters": [
          {
            "name": "sid",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Sid"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SessionUpdate"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SessionRecord"
                }
              }
            }
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            },
            "description": "Not Found"
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ValidationProblem"
                }
              }
            }
          }
        }
      }
    },
    "/v1/sessions/{sid}/messages": {
      "post": {
        "tags": [
          "sessions"
        ],
        "summary": "Send Turn",
        "description": "One turn, run to completion.\n\n**A 409 and never a queue** if a turn is already running: the two callers\nwould otherwise receive each other's turns. **A 504 and never a 200 with\na flag** if the wall clock runs out — the timeout is enforced by killing\nthe process, which is the only way to end a turn on this agent (GP-02).",
        "operationId": "send_turn_v1_sessions__sid__messages_post",
        "parameters": [
          {
            "name": "sid",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Sid"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TurnRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunResponse"
                }
              }
            },
            "headers": {
              "x-sdk-session-id": {
                "schema": {
                  "type": "string"
                },
                "description": "The agent's own conversation id for this turn, echoed so a proxy or relay can recover it without parsing a body or scanning an SSE stream. **Present on the first turn**, including on the streaming route: this build gives the agent an id when it starts the turn, so there is no turn whose id is not yet known. It is absent only when a turn produced no id at all. **It identifies the TURN, not the conversation** -- on a resumed session this build's agent mints a new id every turn, so route on it but never key on it."
              }
            }
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            },
            "description": "Not Found"
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            },
            "description": "Conflict"
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            },
            "description": "Internal Server Error"
          },
          "502": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            },
            "description": "Bad Gateway"
          },
          "503": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            },
            "description": "Service Unavailable"
          },
          "504": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            },
            "description": "Gateway Timeout"
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ValidationProblem"
                }
              }
            }
          }
        }
      }
    },
    "/v1/sessions/{sid}/messages/stream": {
      "post": {
        "tags": [
          "sessions"
        ],
        "summary": "Stream Turn",
        "description": "One turn, as Server-Sent Events.\n\n**This route commits LATE, and `/v1/query/stream` commits early.** The\ndifference is deliberate and a client must know it: here the first event\nis awaited before the response is committed, so `404`, `409` and a\nfirst-event failure are real status codes. Only failures from the second\nevent onward arrive in-band as `event: error`.\n\nA healthy stream always ends with `event: done` carrying the same\n`RunResponse` the non-streaming route would have returned.",
        "operationId": "stream_turn_v1_sessions__sid__messages_stream_post",
        "parameters": [
          {
            "name": "sid",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Sid"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TurnRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "title": "Response Stream Turn V1 Sessions  Sid  Messages Stream Post"
                }
              }
            },
            "headers": {
              "x-sdk-session-id": {
                "schema": {
                  "type": "string"
                },
                "description": "The agent's own conversation id for this turn, echoed so a proxy or relay can recover it without parsing a body or scanning an SSE stream. **Present on the first turn**, including on the streaming route: this build gives the agent an id when it starts the turn, so there is no turn whose id is not yet known. It is absent only when a turn produced no id at all. **It identifies the TURN, not the conversation** -- on a resumed session this build's agent mints a new id every turn, so route on it but never key on it."
              }
            }
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            },
            "description": "Not Found"
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            },
            "description": "Conflict"
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ValidationProblem"
                }
              }
            }
          }
        }
      }
    },
    "/v1/sessions/{sid}/interrupt": {
      "post": {
        "tags": [
          "sessions"
        ],
        "summary": "Interrupt Session",
        "description": "Stop a running turn. **Always 200 with a body**, never 204 or 409.\n\nA turn can finish between a client deciding to stop it and the request\narriving, and that race is unavoidable, so \"there was nothing to stop\"\nis reported in the body rather than as an error.\n\n**Interrupt here is a kill.** Neither interface registers a cancel verb\n(GP-02), so this is abrupt: expect no final event beyond what already\nreached you.",
        "operationId": "interrupt_session_v1_sessions__sid__interrupt_post",
        "parameters": [
          {
            "name": "sid",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Sid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InterruptResult"
                }
              }
            }
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            },
            "description": "Not Found"
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            },
            "description": "Internal Server Error"
          },
          "502": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            },
            "description": "Bad Gateway"
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ValidationProblem"
                }
              }
            }
          }
        }
      }
    },
    "/v1/query": {
      "post": {
        "tags": [
          "query"
        ],
        "summary": "Run Query",
        "description": "Run one turn to completion with no session.\n\n**Consumes no session slot**, which is published rather than assumed:\n`capabilities.query_consumes_a_session_slot` is `false`. It gets its own\nHOME and its own policy all the same — a one-shot turn is not a turn with\na smaller boundary.",
        "operationId": "run_query_v1_query_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/QueryRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "headers": {
              "x-sdk-session-id": {
                "description": "The agent's own conversation id for this turn, echoed so a proxy or relay can recover it without parsing a body or scanning an SSE stream. **Present on the first turn**, including on the streaming route: this build gives the agent an id when it starts the turn, so there is no turn whose id is not yet known. It is absent only when a turn produced no id at all. **It identifies the TURN, not the conversation** -- on a resumed session this build's agent mints a new id every turn, so route on it but never key on it.",
                "schema": {
                  "type": "string"
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunResponse"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "500": {
            "description": "Internal Server Error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "502": {
            "description": "Bad Gateway",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "503": {
            "description": "Service Unavailable",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "504": {
            "description": "Gateway Timeout",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ValidationProblem"
                }
              }
            }
          }
        }
      }
    },
    "/v1/query/stream": {
      "post": {
        "tags": [
          "query"
        ],
        "summary": "Run Query Stream",
        "description": "The same, as Server-Sent Events.\n\n**This route commits EARLY, and that is the whole difference from\n`/v1/sessions/{id}/messages/stream`.** The 200 is written before the\nfirst message exists, so **every** failure — including one that produces\nno events at all — arrives in-band as `event: error` rather than as a\nstatus code. A client that only checks the status here will read a\nfailed turn as a successful one.\n\nIt cannot be otherwise: there is no session to 404 on and no lock to 409\non, so there is nothing to learn before committing that would be worth\nwaiting for.",
        "operationId": "run_query_stream_v1_query_stream_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/QueryRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "title": "Response Run Query Stream V1 Query Stream Post"
                }
              }
            }
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ValidationProblem"
                }
              }
            }
          }
        }
      }
    },
    "/v1/sessions/{sid}/transcript": {
      "get": {
        "tags": [
          "history"
        ],
        "summary": "Get Transcript",
        "description": "A session's recorded events, oldest first.\n\n**Reads STORED rows and asks the agent nothing.** Every turn is its own\nprocess here (GP-41), so once a turn has ended there is nothing left to\nask — this route is the only way to see what happened after the fact,\nand without a database there is no way at all.",
        "operationId": "get_transcript_v1_sessions__sid__transcript_get",
        "parameters": [
          {
            "name": "sid",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Sid"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "maximum": 1000,
              "minimum": 1,
              "default": 200,
              "title": "Limit"
            }
          },
          {
            "name": "after",
            "in": "query",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "integer"
                },
                {
                  "type": "null"
                }
              ],
              "title": "After"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TranscriptPage"
                }
              }
            }
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            },
            "description": "Not Found"
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ValidationProblem"
                }
              }
            }
          }
        }
      }
    },
    "/v1/runs/{run_id}": {
      "get": {
        "tags": [
          "history"
        ],
        "summary": "Get Run",
        "description": "One recorded turn, by the id this service minted for it.\n\n**`run_id` is neither of the session ids.** It identifies a single turn,\nit is returned by nothing on the live path today, and it is discoverable\nfrom a transcript row — which is why an unknown one is a plain 404.",
        "operationId": "get_run_v1_runs__run_id__get",
        "parameters": [
          {
            "name": "run_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "title": "Run Id"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful Response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StoredRun"
                }
              }
            }
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            },
            "description": "Not Found"
          },
          "422": {
            "description": "Request validation failed",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ValidationProblem"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "Accepts": {
        "properties": {
          "permission_modes": {
            "items": {
              "$ref": "#/components/schemas/SessionMode"
            },
            "type": "array",
            "title": "Permission Modes",
            "description": "The permission modes THIS BUILD honours, each with an id, a short name and a description of what it actually permits. **Read this rather than assuming a vocabulary**: it was a list of strings taken from one SDK's enum, which every implementation had to accept whether or not it could honour them. A build declares only what it has, and refuses an undeclared id with 400. `default` and `plan` are well-known ids a build uses where it has an equivalent, so one payload can work against more than one implementation; a build with no equivalent omits them rather than mapping them onto something that does not mean the same thing."
          },
          "effort_levels": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Effort Levels",
            "description": "Reasoning levels this build delivers **exactly** -- a level it silently maps onto another is not listed.\n\n**Empty means `RunOptions.effort` is refused with a 400**: hide the control rather than rendering an empty dropdown."
          },
          "setting_sources": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Setting Sources",
            "description": "Ambient configuration layers a caller may ask this build to load -- `user`, `project`, `local` where they exist.\n\n**Empty means `RunOptions.setting_sources` is refused**, so nothing you send changes what that build reads from its disk.\n\n**A switch only.** Memory files, skills, subagents, commands and plugins are read from inside the container, and no field on any build can supply them."
          },
          "default_allowed_tools": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Default Allowed Tools",
            "description": "What a session gets when `RunOptions.allowed_tools` is omitted -- **not the list of tool names that exist**, which is wider on every build.\n\nA starting selection, never a vocabulary to validate against: an unrecognised name is ignored rather than refused. **`[]` means the build governs by sandbox** and refuses `allowed_tools` outright."
          },
          "always_disallowed_tools": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Always Disallowed Tools",
            "description": "Tools removed from every session whatever a caller sends.\n\n**A filter, not a refusal**: naming one in `allowed_tools` is not an error -- it is dropped and the session proceeds, so this list is the only notice you get. Render them as disabled options."
          },
          "unsupported_options": {
            "items": {
              "$ref": "#/components/schemas/UnsupportedOption"
            },
            "type": "array",
            "title": "Unsupported Options",
            "description": "`RunOptions` fields this deployment cannot honour. Sending one is a **400**, never a silent drop.\n\n**AS-32, and it is the general case the two booleans above are special cases of.** A field the caller sets and nothing acts on produces a request that succeeds while doing something other than what it says -- the agent runs without the tools or the budget the caller believes it has, and the failure surfaces as an agent that is inexplicably bad at its job. That is the failure this list exists to convert into a refusal a caller can read in advance.\n\n**Empty means every `RunOptions` field is honoured**, which is the value a build serves when its SDK covers the whole surface. A non-empty entry is either a build fact (the SDK has no equivalent and this service cannot supply one) or a deployment setting -- `mcp_servers` appears here exactly when `allow_mcp_servers` is `false`, and the two never disagree.\n\nOptions this service enforces ITSELF are absent from this list even when the SDK has no equivalent -- what the caller asked for happens, and by what means is not the caller's business.",
            "examples": [
              [],
              [
                {
                  "field": "max_turns"
                },
                {
                  "field": "system_prompt",
                  "types": [
                    "object"
                  ]
                }
              ]
            ]
          },
          "strict_mcp_config": {
            "type": "boolean",
            "title": "Strict Mcp Config",
            "description": "The server-side default for `RunOptions.strict_mcp_config`. **True here, which is NOT the SDK's default of false**: the workspace is agent-writable, so a `.mcp.json` in it would otherwise add servers the caller never sent."
          },
          "allow_supplied_sdk_session_id": {
            "type": "boolean",
            "title": "Allow Supplied Sdk Session Id",
            "description": "Whether `POST /v1/sessions` accepts a caller-supplied `sdk_session_id`. **Check this before sending one** -- the same pre-flight `allow_mcp_servers` and `credential_sources` exist for.\n\n| Value | `POST /v1/sessions` with `sdk_session_id` |\n| --- | --- |\n| `true` | adopts it and returns it on the 201, before any model call |\n| `false` | **400** with a problem document saying why |\n\n**`false` is never a silent drop.** A build that took the field and returned a different id would break the one guarantee supplying it provides -- that the caller's id names the conversation -- and would break it invisibly.\n\nIt is `false` when the underlying SDK mints its own conversation id with no way to override it. Such a build still reports an `sdk_session_id`; it simply chooses the value. Reading this field is how a caller decides whether to generate an id or to take the one it is given."
          },
          "mcp": {
            "$ref": "#/components/schemas/Mcp",
            "description": "Which MCP servers this build can express, beyond whether it takes them at all (`allow_mcp_servers`). **AS-32**: the two builds differ here and a client must act on the difference before it sends a server, not after."
          },
          "limits": {
            "additionalProperties": {
              "type": "number"
            },
            "type": "object",
            "title": "Limits",
            "description": "Ceilings and defaults on what a REQUEST may ask for, by name: `default_*` is what you get by omitting a field, `max_allowed_*` is the most you may send. **Above a ceiling is a 400, never a clamp.**\n\n**Free-form on purpose** -- a build publishes the ones it has, and a name absent here is a limit this build does not impose. Figures the build enforces on its own live in `behaviour.limits`; the split is why a key prefix is no longer the only clue which kind you are reading."
          }
        },
        "type": "object",
        "required": [
          "permission_modes",
          "effort_levels",
          "setting_sources",
          "default_allowed_tools",
          "always_disallowed_tools",
          "unsupported_options",
          "strict_mcp_config",
          "allow_supplied_sdk_session_id",
          "mcp",
          "limits"
        ],
        "title": "Accepts",
        "description": "What a caller may SEND: vocabularies, refusals and ceilings.\n\n**This group is exactly what `GET /v1/schemas/run-options` renders as JSON\nSchema.** The two are one fact in two shapes -- this one branchable, that one\nvalidatable -- and a conformance clause asserts they agree."
      },
      "AgentEvent": {
        "properties": {
          "seq": {
            "type": "integer",
            "title": "Seq",
            "description": "Position within THIS turn, increasing from zero. **Order by it; do not count with it and do not compare it across turns** -- it is an index into one turn's event list, not a cursor over the session."
          },
          "type": {
            "type": "string",
            "enum": [
              "system",
              "assistant",
              "user",
              "result",
              "stream_event",
              "rate_limit",
              "unknown"
            ],
            "title": "Type",
            "description": "What kind of message this is, from a closed set the specification owns. **This is the authoritative discriminator and the SSE frame name is NOT** -- the `event:` line of a streamed frame is an implementation detail that differs between builds, so a client reading it instead of this field works on some and silently renders nothing on others. Neither streaming route can be consumed with `EventSource` anyway, since both are POST."
          },
          "subtype": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Subtype",
            "description": "The originating agent's own sub-kind for this event -- `init` on a system frame, for example.\n\n**Another vendor's vocabulary, passed through.** Useful for display and for debugging; **branch on `type`**, which is this specification's own and is the same on every build."
          },
          "content": {
            "anyOf": [
              {
                "items": {
                  "additionalProperties": true,
                  "type": "object"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Content",
            "description": "The message's content, NORMALISED, as a list of blocks. Each block is an object with a `type` discriminator; a block of type `text` carries its string in `text`. This is what a client renders a conversation from -- `raw` is the originating SDK's own payload and its shape differs per build. **`null` means this event carries no renderable content** (an `init` frame, a rate-limit notice), never that the content was dropped. Where an implementation has text to report it fills this, so that a caller reading only this field sees the whole conversation on every build."
          },
          "raw": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "title": "Raw",
            "description": "The agent's own payload for this event, verbatim.\n\n**Shape differs per build and is not part of this contract** -- for debugging; depend on `content` and `type` instead. Present when `RunOptions.include_raw` asked for it, and it is the bulk of an event, so turn it off for transcript rendering."
          }
        },
        "type": "object",
        "required": [
          "seq",
          "type"
        ],
        "title": "AgentEvent",
        "description": "One SDK message, normalized.\n\nNo session_id: it is not uniformly available across message types, so it is\nreported once per run on RunResponse instead."
      },
      "Behaviour": {
        "properties": {
          "limits": {
            "additionalProperties": {
              "type": "number"
            },
            "type": "object",
            "title": "Limits",
            "description": "Figures this build ENFORCES on its own, by name -- `session_idle_ttl_s` closes an idle session, `turn_timeout_s` ends a turn. **A promise about behaviour, not a constraint on your request**: nothing here is something you send, and nothing here answers a 400.\n\nCeilings on what you may ask for are `accepts.limits`. A measured characteristic that nothing enforces is neither -- see `turn_token_overhead`."
          },
          "mcp_tool_call": {
            "$ref": "#/components/schemas/McpToolCall",
            "description": "What holds an MCP tool call open on this build, and what ends it. **AS-32**: a deliberately long tool call succeeds on one build, fails with a named timeout on another and fails with a bare transport error on a third, and the difference is a build fact a client must act on before it offers a server rather than after.\n\n**Requested by Agent Harness (2026-08-18)**, which hosts an MCP server whose first tool holds the call open until another agent replies -- so a long call is the feature rather than an edge case, and the deadline it must choose has to be a number."
          },
          "sandbox": {
            "$ref": "#/components/schemas/Sandbox",
            "description": "What confines the AGENT's own tools inside the container. **AS-32**: the two implementations differ here and an Agent that shells out notices immediately.\n\nDistinct from `permission_enforcement`, which is about how a request's `permission_mode` is applied. This is about what exists to apply it with."
          },
          "permission_enforcement": {
            "type": "string",
            "enum": [
              "none",
              "hook"
            ],
            "title": "Permission Enforcement",
            "description": "Whether THIS SERVICE inspects each tool call in-process before it runs. That is the only question this field answers.\n\n**\"none\" does NOT mean the agent is unconfined, and two builds reporting it do not confine alike.** It means there is no in-process per-call check. A build may still confine by the container alone, by an OS-level sandbox around each turn, or by a tool policy the agent loads when a session opens -- none of those is an in-process hook, so every one of them reports `none`.\n\n\"hook\": this service inspects each call and can refuse it while the turn is running. Even then the coverage may be partial: a hook that confines file writes need not confine a shell, and a shell redirect bypasses such a hook entirely.\n\n**Do not compare two builds on this field alone.** Read it with `sandbox.confines_writes_to_workspace`, `sandbox.network_access`, `default_allowed_tools` and `always_disallowed_tools` -- that is where the difference shows. Two builds can both report `none` while one enables a shell by default and the other refuses one whatever the caller asks for.\n\n**A second dimension is missing, and known to be missing**: WHEN the boundary is fixed. A policy the agent loads at session open cannot be narrowed mid-turn; an in-process check can. Both report through this single field today."
          },
          "model_usage_scope": {
            "type": "string",
            "enum": [
              "per_turn",
              "cumulative",
              "not_reported"
            ],
            "title": "Model Usage Scope",
            "description": "How to aggregate `model_usage` across the turns of ONE session. **The shape is identical either way, so this cannot be inferred from a response** -- same field name, same type, same nesting on every build.\n\n`\"per_turn\"`: each turn reports its own figures. **Sum them** to get a session total.\n\n`\"cumulative\"`: each turn reports the running total for the agent connection. **Difference consecutive turns** to get a turn's own figures; summing double-counts every earlier turn.\n\n`\"not_reported\"`: this build emits `model_usage: null` on every turn and every session. **Skip it** -- neither of the other instructions is right, and a scanner looking for a key that is never there records nothing rather than something wrong.\n\n**This is about `model_usage` and NOT about the cost fields.** On a `cumulative` build `turn_cost_usd` is already differenced by this service while `model_usage` is a verbatim pass-through that is not, so one response can carry per-turn money beside cumulative tokens. That asymmetry is the reason the field exists.\n\n**Required with no default**, on the same reasoning as `sdk_session_id_scope`: a build that cannot say what its numbers are scoped to should fail to construct this payload rather than answer with a reassuring value nobody measured.",
            "examples": [
              "per_turn"
            ]
          },
          "reports_cost_usd": {
            "type": "boolean",
            "title": "Reports Cost Usd",
            "description": "Whether this build's agent reports a monetary figure at all.\n\n**`false` distinguishes *never priced* from *not priced yet*, which `null` alone cannot.** The cost fields are nullable for both reasons -- a build whose agent reports tokens and latency and no currency answers `null` permanently and by nature, while a build that does price a turn answers `null` only until it has. A client hides a column for the first and shows it empty for the second, and could not tell them apart from a response.\n\n`false` says `total_cost_usd` and `turn_cost_usd` are `null` on every session and every turn of this build, permanently and not by configuration. `0.0` would read as *free*, which is why those fields are null rather than zero.",
            "examples": [
              true
            ]
          },
          "usage_counts_tool_calls": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Usage Counts Tool Calls",
            "description": "Whether a tool-call count in `usage` can be trusted to mean what it says.\n\n**`false` is a real answer and one build measures it**: a turn carrying both a `tool_use` and a `tool_result` reported zero, because the counter counts only calls that reached a registered tool. A client deciding whether a turn used tools must count `tool_use` EVENTS on such a build.\n\n`null` means unmeasured rather than reliable, because `usage` is a verbatim pass-through and no build owes it a meaning it never promised."
          },
          "turn_token_overhead": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Turn Token Overhead",
            "description": "Approximate input tokens a turn costs BEFORE the caller's prompt is counted -- the agent's own system prompt and tool declarations.\n\n**Published because it changes how a client should batch.** Where this is large, TURN COUNT predicts spend rather than prompt length, and a client sending many small turns pays far more than one that batches. A measured example: three prompts of three to nine words costing 7,072, 7,076 and 7,082 input tokens.\n\n**`null` means this build has not measured it**, never that the overhead is zero. Approximate by nature: it moves with the agent's version and its tool set."
          },
          "llm_correlation": {
            "$ref": "#/components/schemas/LlmCorrelation",
            "description": "How a gateway can join THIS BUILD'S AGENT's model traffic to a session -- the header it sends, and whether anyone has looked.\n\n**Requested by Agent Harness (2026-08-12) as the highest-value of four**, because it is the one that blocks a feature rather than risking a wrong number: a gateway that is the model endpoint for every container has one vendor's header name compiled in and no way to discover the others."
          },
          "sdk_session_id_scope": {
            "type": "string",
            "enum": [
              "turn",
              "conversation"
            ],
            "title": "Sdk Session Id Scope",
            "description": "What `sdk_session_id` is an id OF on this build. **The value looks identical either way, so this cannot be inferred from a response.**\n\n`\"conversation\"`: it identifies the conversation and is stable across ordinary turns, so a client may key on it. It may still change under an explicit fork or resume on some builds -- stable is not immutable.\n\n`\"turn\"`: **it identifies ONE TURN.** A build whose agent mints a new id each time it resumes reports this. Route on it if you like; **never key on it**, because the next turn of the same conversation answers with a different value.\n\n**`session_id`, this service's own, is the stable handle on every build** and is what every path takes. This field exists so a client that also wants the agent's id knows what it is holding.",
            "examples": [
              "conversation"
            ]
          },
          "query_reports_sdk_session_id": {
            "type": "boolean",
            "title": "Query Reports Sdk Session Id",
            "description": "Whether `POST /v1/query` sends the `x-sdk-session-id` response header.\n\n**AS-32, and the reason this field exists rather than being left to a document diff.** A relay routing on the SDK's conversation id can read it off the header when this is `true` and must scan the body when it is `false` -- which is a branch in the caller, so it is published where a running caller reads it.\n\nIt is `true` on a build whose SDK assigns the conversation id when the session opens, and `false` where the id arrives with the first turn -- by which time a one-shot response has already been committed. The two session turn routes send the header on every build and are not covered by this field."
          },
          "query_consumes_a_session_slot": {
            "type": "boolean",
            "title": "Query Consumes A Session Slot",
            "description": "Whether `POST /v1/query` counts against `max_sessions` while it runs, and can therefore answer **429**.\n\n**AS-32.** A caller needs to know whether its one-shot traffic competes with its sessions for the same cap, and whether a retry-on-429 path is reachable on this route at all. `true` on a build that implements the one-shot as a real throwaway session."
          }
        },
        "type": "object",
        "required": [
          "limits",
          "mcp_tool_call",
          "sandbox",
          "permission_enforcement",
          "model_usage_scope",
          "reports_cost_usd",
          "llm_correlation",
          "sdk_session_id_scope",
          "query_reports_sdk_session_id",
          "query_consumes_a_session_slot"
        ],
        "title": "Behaviour",
        "description": "What this build DOES and REPORTS -- facts to design around, not to send.\n\nNothing here constrains a request. It decides how a client reads a result,\nbills a turn, correlates a log line, or plans around a timeout."
      },
      "Config": {
        "properties": {
          "workspace_dir": {
            "type": "string",
            "title": "Workspace Dir",
            "description": "Absolute path **inside the container** that the agent works in, and what `RunOptions.working_directory` resolves against.\n\n**Mount a host directory here** or everything the agent writes dies with the container; `require_mounts` says whether this build refuses to start without one.\n\nWhatever ambient configuration that directory carries -- a memory file, a settings file -- is read on every turn."
          },
          "reference_dirs": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Reference Dirs",
            "description": "Extra read-only directories the agent may read besides the workspace.\n\n**Empty means the build has no such mechanism wired** -- a fact about the build, not the deployment. No `RunOptions` field carries it, which is why it is published as `[]` rather than as a refusal nobody could trigger."
          },
          "default_model": {
            "type": "string",
            "title": "Default Model",
            "description": "Model used when `RunOptions.model` is omitted.\n\n**Deployment-configured, not a property of the build** -- two containers of one image answer differently, so cache this payload per instance rather than against `impl.version`."
          },
          "max_sessions": {
            "type": "integer",
            "title": "Max Sessions",
            "description": "The concurrent-session cap. `POST /v1/sessions` answers 429 once this many are open, and this is the same value that request's message is formatted from -- so it is the denominator for \"N of M sessions\", whose numerator is `GET /v1/sessions`. Previously reachable only by scraping the prose of a 429."
          },
          "require_credentials": {
            "type": "boolean",
            "title": "Require Credentials",
            "description": "Whether this build refuses to BOOT when none of `credential_sources`/`provider_selectors` is set (exit 3). True is the default. False means the service will start without a credential -- for docs-only use -- and fail on the first turn instead."
          },
          "require_mounts": {
            "type": "boolean",
            "title": "Require Mounts",
            "description": "Whether this build refuses to BOOT when `workspace_dir` is not on a mounted filesystem, or a `reference_dirs` entry does not exist (exit 3). True is the default. Published for the same reason as `require_credentials`: if a boot check can fail, a caller that starts containers is better off knowing what it checks than discovering it from an exit code."
          },
          "auth_required": {
            "type": "boolean",
            "title": "Auth Required",
            "description": "Whether `/v1` requires a bearer credential.\n\n**It authenticates the caller to this instance and is not an identity.** This service does not know which user is behind a request, and nothing is scoped by the token.\n\n**Use a per-instance token.** A token this service holds is readable by the agent it runs, so one shared across a fleet is readable by anyone who can take a single turn."
          },
          "allow_mcp_servers": {
            "type": "boolean",
            "title": "Allow Mcp Servers",
            "description": "Whether `RunOptions.mcp_servers` is accepted. True is the default. **False makes a request carrying MCP servers a 400** rather than silently dropping them, so check this before relying on the field -- the same pre-flight `credential_sources` exists for.\n\nAn operator turns this off for ATTRIBUTION, not for capability: a `stdio` server is a subprocess spawn, and on a build whose agent has a shell tool that agent already spawns whatever it likes. The difference is that a tool call is the agent's decision and lands in the transcript, while a stdio server starts with the session, before any prompt, and appears in no turn's events.\n\n**On a build with no shell the argument is stronger, not weaker**: there a stdio server is the only way a caller can cause a process to start at all."
          },
          "credential_sources": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Credential Sources",
            "description": "The environment variables this build accepts as a credential for ITS OWN agent -- each build wraps a different vendor, so the names here are that vendor's -- in the order it checks them. Check against this before starting a container.\n\n**This is what the BOOT GATE accepts**, which is a presence check on these names -- not a claim about every credential the bundled CLI could authenticate with. A credential supplied by any other route does not satisfy it and the service refuses to start."
          },
          "provider_selectors": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "title": "Provider Selectors",
            "description": "Environment variables that select a cloud provider instead of supplying a credential. **Setting one satisfies the boot gate and authenticates nothing by itself** -- the credential then comes from that provider's own chain (AWS, GCP, Azure), which this service does not inspect. Published separately from `credential_sources` so that injecting one is not mistaken for a way to deliver a key.\n\n**\"Provider\" here means the cloud that HOSTS the model** -- Bedrock, Vertex, Foundry. It does NOT mean which agent SDK this build wraps; that is `sdk.name`.\n\n**Selectors only, and no URL.** This API has no endpoint override. A deployment that needs one sets it in the container's environment, where this service can neither see nor report it."
          }
        },
        "type": "object",
        "required": [
          "workspace_dir",
          "reference_dirs",
          "default_model",
          "max_sessions",
          "require_credentials",
          "require_mounts",
          "auth_required",
          "allow_mcp_servers",
          "credential_sources",
          "provider_selectors"
        ],
        "title": "Config",
        "description": "How THIS instance is set up -- the operator's choices, not the build's.\n\n**Two containers of one image answer differently here**, which is the whole\nreason this payload is cached per instance and keyed by the base URL rather\nthan by a version."
      },
      "ContextUsage": {
        "properties": {
          "categories": {
            "items": {
              "additionalProperties": true,
              "type": "object"
            },
            "type": "array",
            "title": "Categories"
          }
        },
        "type": "object",
        "title": "ContextUsage",
        "description": "Raw passthrough of ClaudeSDKClient.get_context_usage().\n\nCategories are reported by the CLI (System tools, Skills, Messages,\nAutocompact buffer, Free space, ...) and their exact set is not part of\nany contract, so they are carried through unmodelled."
      },
      "Deployment": {
        "properties": {
          "service": {
            "$ref": "#/components/schemas/Service"
          },
          "config": {
            "$ref": "#/components/schemas/Config"
          },
          "accepts": {
            "$ref": "#/components/schemas/Accepts"
          },
          "behaviour": {
            "$ref": "#/components/schemas/Behaviour"
          }
        },
        "type": "object",
        "required": [
          "service",
          "config",
          "accepts",
          "behaviour"
        ],
        "title": "Deployment",
        "description": "Everything true of the service instance you are talking to.\n\n**Four groups because four different questions were being answered by one\nflat object**, and a reader could not tell which was which: a version, an\noperator's setting, a constraint on the request and a fact about behaviour\nall sat side by side. `limits` and `mcp` straddled the line and were cut.\n\n**Renamed from `Capabilities` at `/v1/capabilities`.** Half of what it\ncarries was never a capability -- `workspace_dir` and `max_sessions` are\nconfiguration -- and the name misled accordingly."
      },
      "HTTPValidationError": {
        "properties": {
          "detail": {
            "items": {
              "$ref": "#/components/schemas/ValidationError"
            },
            "type": "array",
            "title": "Detail"
          }
        },
        "type": "object",
        "title": "HTTPValidationError"
      },
      "Health": {
        "properties": {
          "status": {
            "type": "string",
            "const": "ok",
            "title": "Status"
          },
          "credentials_configured": {
            "type": "boolean",
            "title": "Credentials Configured"
          },
          "workspace_dir": {
            "type": "string",
            "title": "Workspace Dir"
          },
          "auth_required": {
            "type": "boolean",
            "title": "Auth Required",
            "description": "Whether `/v1` requires `Authorization: Bearer <token>`. **This route never does**, so it is always answerable — the container healthcheck reads it, and an authenticated service whose healthcheck could not run would be permanently unhealthy.\n\n`false` means the deployment serves `/v1` to anyone who can reach the port, on a service whose documented capability is arbitrary shell execution. That is a supported configuration for a single-operator loopback deployment and nothing else. Put network isolation and a relay in front of it before a token."
          },
          "database_configured": {
            "type": "boolean",
            "title": "Database Configured",
            "description": "Whether `AGENT_SERVICE_DATABASE_URL` is set. Persistence is optional; `false` is a normal, supported deployment in which the history routes answer 404 with `type: .../persistence-disabled`."
          },
          "database_usable": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Database Usable",
            "description": "Whether this service can actually query its database right now, or `null` when none is configured. Probed per request against a real table, so it covers an unreachable host, a rejected credential AND a schema that was never migrated -- the last of which a `SELECT 1` would miss and which is the default first state of a deployment that enables persistence, because migrations do not run on startup.\n\n**`status` stays `ok` when this is `false`.** The service is up and serving agent traffic; persistence is an optional subsystem and its failure is deliberately not allowed to stop turns. What it does mean is that rows are being **discarded** -- the writer cannot raise into a turn, so it counts and drops -- and that the history routes will answer 500. Alert on this field, not on the status code."
          }
        },
        "type": "object",
        "required": [
          "status",
          "credentials_configured",
          "workspace_dir",
          "auth_required",
          "database_configured",
          "database_usable"
        ],
        "title": "Health"
      },
      "Impl": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name",
            "description": "The implementation's name, e.g. `claude-python`. Distinct from `sdk.name`: two implementations could wrap the same SDK from different languages.",
            "examples": [
              "claude-python"
            ]
          },
          "version": {
            "type": "string",
            "title": "Version",
            "description": "This build's version, and the tag its image is published under. See `spec.document_version` for the interface it implements.",
            "examples": [
              "0.16.0"
            ]
          }
        },
        "type": "object",
        "required": [
          "name",
          "version"
        ],
        "title": "Impl",
        "description": "Which build this is, and at what version."
      },
      "InterruptResult": {
        "properties": {
          "interrupted": {
            "type": "boolean",
            "title": "Interrupted",
            "description": "True only if a control request was genuinely sent to the agent. False means there was nothing to interrupt -- not an error. This is NOT implied by `status`: a turn abandoned mid-drain (the usual result of a dropped SSE connection) leaves the session `idle` while the agent process is still producing, and interrupting it does send a real control request."
          },
          "status": {
            "type": "string",
            "enum": [
              "idle",
              "running",
              "closed"
            ],
            "title": "Status",
            "description": "The session's status immediately AFTER the request. A turn can end while the control request is in flight, so this may differ from the status the request arrived to find."
          }
        },
        "type": "object",
        "required": [
          "interrupted",
          "status"
        ],
        "title": "InterruptResult",
        "description": "What an interrupt request actually did.\n\nInterrupting is idempotent and never an error: a turn can end between a\ncaller deciding to stop it and the request arriving, which is a race no\nclient can avoid. Hence 200 rather than 409 -- and a body rather than a\nbare 204, which would report success for a request that did nothing."
      },
      "LlmCorrelation": {
        "properties": {
          "header": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Header",
            "description": "The HTTP header the agent sends on its own model-API requests whose value equals `sdk_session_id`, or `null` for none.\n\n**Read `measured` first.** `null` with `measured: true` is a finding: this agent sends no such header and a gateway must attribute its spend some other way. `null` with `measured: false` is an absence of knowledge and promises nothing in either direction.\n\n**Where it is a string, the equality is measured on the wire** and not inferred from a name that looks right. A header carrying some id is useless to a gateway unless it is the same string this service reports.",
            "examples": [
              "x-claude-code-session-id",
              null
            ]
          },
          "measured": {
            "type": "boolean",
            "title": "Measured",
            "description": "Whether this build has actually watched the agent's traffic.\n\n**`true` means a request was captured** -- the endpoint variable was pointed at a local sink, a turn was taken, and the headers were read. It is what makes `header: null` a finding rather than a gap.\n\n**`false` means nobody has looked**, and this service will not fill the field in from a vendor's documentation. A build can be here because the measurement is not yet done or because the interface it drives could not be exercised where the probe runs.",
            "examples": [
              true
            ]
          }
        },
        "type": "object",
        "required": [
          "header",
          "measured"
        ],
        "title": "LlmCorrelation",
        "description": "How an agent's own model traffic can be joined to a session, if at all.\n\n**A property of the AGENT BINARY rather than of this service**, on the\n`endpoint_source` precedent: only the side that can put a proxy in front of\nthe agent is positioned to measure it, and a client otherwise hard-codes one\nvendor's header name and gets nothing from the others.\n\n**What it is for.** A deployment that puts a gateway between the container\nand the model endpoint sees the AGENT's requests, not this service's, so it\ncan attribute spend to a session only by reading a correlation id off the\nagent's own request.\n\n**Two fields because there are three answers, and one nullable string can\nonly carry two.** This was proposed as a bare `llm_correlation_header:\nstring | null` and the measurement is what refuted it: one build sends a\nheader, one provably sends none, and one has not been measured through the\ninterface it actually drives. *Sends none* and *not looked* are different\ninstructions to a consumer -- the first says stop, the second says this may\nyet be worth measuring -- so the shape has to distinguish them."
      },
      "Mcp": {
        "properties": {
          "transports": {
            "items": {
              "type": "string",
              "enum": [
                "stdio",
                "sse",
                "http"
              ]
            },
            "type": "array",
            "title": "Transports",
            "description": "The `McpServer.type` values this build can reach. **A server of any other type is a 400**, named and refused before a session exists, rather than configured and silently unreachable.\n\n`http` is streamable HTTP. A build listing `http` and not `sse` has no SSE transport at all -- the two are different protocols and one does not stand in for the other.",
            "examples": [
              [
                "stdio",
                "sse",
                "http"
              ],
              [
                "stdio",
                "http"
              ]
            ]
          },
          "http_headers": {
            "type": "string",
            "enum": [
              "any",
              "bearer_only",
              "none"
            ],
            "title": "Http Headers",
            "description": "What an `http` or `sse` server's `headers` may contain. `any` forwards every header; **`bearer_only` accepts exactly one, `Authorization: Bearer ...`, and refuses anything else with a 400**; `none` cannot send headers at all.\n\n**`bearer_only` is a real limit and not a security posture**: it is what the underlying agent runtime can carry. A caller whose MCP server authenticates with an API-key header cannot use it on such a build, and finding that out from this field is cheaper than finding it out from a tool that never works.",
            "examples": [
              "any",
              "bearer_only"
            ]
          },
          "server_name_pattern": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Server Name Pattern",
            "description": "A regular expression every key of `RunOptions.mcp_servers` must match on this build. **A name that does not match is a 400 before a session exists**, not a server that quietly never answers.\n\n**`null` means this build enforces no pattern of its own**, on the convention `limits` already sets -- what is absent there is a limit the build does not impose. It is deliberately NOT the null-means-unmeasured rule the nullable measurements follow, because this is a statement about THIS SERVICE's check: null does not promise the underlying agent accepts every name, only that nothing here refuses one.\n\n**Requested by Agent Harness (2026-08-12), and the failure is a stored-capability one.** A consumer that stores a server definition once and sends it to whichever build an Agent runs on has nothing to validate against at storage time, so a name that works on two builds and is refused by the third fails at session create -- long after the author who could fix it has gone. A pattern moves that refusal to publish time.\n\nOne build publishes `^[^_]+$` because its agent parses an MCP tool name as `mcp_<server>_<tool>` by splitting on the first underscore, so a server whose name contains one can never be addressed.",
            "examples": [
              "^[^_]+$",
              null
            ]
          }
        },
        "type": "object",
        "required": [
          "transports",
          "http_headers"
        ],
        "title": "Mcp",
        "description": "What a build can express of `RunOptions.mcp_servers`.\n\n**AS-32, and `allow_mcp_servers` is not enough on its own.** That boolean\nsays whether MCP servers are accepted at all; this says which of them are.\nA client whose Agent's capability is an `sse` server needs to know before it\nsends one, and the difference is a build fact rather than a deployment\nsetting -- one SDK has three transports and another has two."
      },
      "McpHttpServer": {
        "properties": {
          "type": {
            "type": "string",
            "const": "http",
            "title": "Type"
          },
          "url": {
            "type": "string",
            "minLength": 1,
            "title": "Url",
            "examples": [
              "https://mcp.example.com/mcp"
            ]
          },
          "headers": {
            "anyOf": [
              {
                "additionalProperties": {
                  "type": "string"
                },
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "title": "Headers",
            "description": "Sent to `url` on every request. Never logged by this service."
          }
        },
        "type": "object",
        "required": [
          "type",
          "url"
        ],
        "title": "McpHttpServer",
        "description": "An MCP server reached over HTTP. No subprocess."
      },
      "McpSseServer": {
        "properties": {
          "type": {
            "type": "string",
            "const": "sse",
            "title": "Type"
          },
          "url": {
            "type": "string",
            "minLength": 1,
            "title": "Url",
            "examples": [
              "https://mcp.example.com/sse"
            ]
          },
          "headers": {
            "anyOf": [
              {
                "additionalProperties": {
                  "type": "string"
                },
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "title": "Headers",
            "description": "Sent to `url` on every request. The other position ADR-0023 substitutes a secret into. Never logged by this service."
          }
        },
        "type": "object",
        "required": [
          "type",
          "url"
        ],
        "title": "McpSseServer",
        "description": "An MCP server reached over SSE. No subprocess."
      },
      "McpStdioServer": {
        "properties": {
          "type": {
            "type": "string",
            "const": "stdio",
            "title": "Type",
            "default": "stdio"
          },
          "command": {
            "type": "string",
            "minLength": 1,
            "title": "Command",
            "examples": [
              "npx"
            ]
          },
          "args": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Args",
            "examples": [
              [
                "-y",
                "@acme/mcp"
              ]
            ]
          },
          "env": {
            "anyOf": [
              {
                "additionalProperties": {
                  "type": "string"
                },
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "title": "Env",
            "description": "Environment for the SPAWNED SERVER's process only -- it cannot reach the agent's CLI. **Treat as credential material** in logs and storage. This service never logs it."
          }
        },
        "type": "object",
        "required": [
          "command"
        ],
        "title": "McpStdioServer",
        "description": "An MCP server this service SPAWNS AS A SUBPROCESS.\n\nIt starts with the session, before any prompt, and appears in no turn's\nevents -- unlike a tool call, which is the agent's own decision and is\nrecorded. Operators who need every process start attributable to a turn set\n`AGENT_SERVICE_ALLOW_MCP_SERVERS=false`, published as\n`Capabilities.allow_mcp_servers`."
      },
      "McpToolCall": {
        "properties": {
          "request_timeout_s": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Request Timeout S",
            "description": "How long the client waits for the response to **begin**, or `null` for no bound.\n\n**Satisfied by responding, not by finishing.** On a streaming transport the clock stops the moment the server sends its SSE headers -- the request has been served and what follows is governed by `idle_timeout_s` and `total_timeout_s`. A server that buffers its whole answer and replies with one JSON body is the shape this bound refuses.\n\n**Where a build publishes a number it is a floor rather than a setting**: the agent takes the larger of it and any override, so it can be raised and never lowered.",
            "examples": [
              60,
              null
            ]
          },
          "idle_timeout_s": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Idle Timeout S",
            "description": "How long the call may go **between frames** once the response has begun, or `null` for no bound.\n\n**An SSE comment is not a frame that counts** on the build that publishes a number here -- its own error names what does: *no response or progress*. A keepalive-only stream therefore dies at exactly this figure while looking healthy on the wire, which is the most expensive way to discover the difference.\n\nRead it together with `progress_resets_idle`, which says whether `notifications/progress` is one of the frames that counts.",
            "examples": [
              300,
              null
            ]
          },
          "total_timeout_s": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Total Timeout S",
            "description": "**Wall clock for the whole call, regardless of activity**, or `null` for no bound.\n\n**This is the deadline, and it is the only one of the three a well-behaved server cannot satisfy by behaving well.** Responding at once and emitting progress throughout clears the other two and does nothing to this one. A client that needs a call held longer than this figure cannot get it from any MCP-level behaviour on this build.\n\n**Bounded again by the run.** A tool call lives inside a turn, so the effective ceiling is the smaller of this and the request's own `timeout_s` -- itself capped by `limits.max_allowed_timeout_s`. On one build the two figures coincide, which is not slack.",
            "examples": [
              100000,
              600,
              null
            ]
          },
          "progress_resets_idle": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Progress Resets Idle",
            "description": "Whether `notifications/progress` restarts `idle_timeout_s`.\n\n**`null` is a third answer and not a missing one**: it means this build has no bound of any kind, so there is nothing for progress to reset and neither `true` nor `false` states it honestly. Where any of the three timers exists, this is a boolean.\n\n**`false` is the value that costs a client something, and it is not the same as *progress is ignored*.** A build may ask for progress -- sending a `progressToken` on every call -- and use it only to drive its own display while a flat `total_timeout_s` runs underneath. Emitting progress there buys nothing, and a client that measured a short call and concluded otherwise has published a coincidence as a promise.",
            "examples": [
              true,
              false,
              null
            ]
          }
        },
        "type": "object",
        "required": [
          "request_timeout_s",
          "idle_timeout_s",
          "total_timeout_s",
          "progress_resets_idle"
        ],
        "title": "McpToolCall",
        "description": "What holds an MCP tool call open on this build, and what ends it.\n\n**AS-32, and the failure it prevents is the quiet kind.** A client whose MCP\ntool deliberately runs for minutes -- waiting on a human, on another agent,\non a queue -- gets three different outcomes from one mistake across three\nbuilds: a named timeout, a bare transport error, and a success. Nothing in\nthe other `mcp` fields says which, and an experiment can only report what a\nclient happened not to exceed.\n\n**THREE timers rather than two, because each build is bounded by a different\none.** They are not interchangeable and a server satisfies them by different\nmeans: `request_timeout_s` is cleared by *responding*, `idle_timeout_s` by\n*continuing to say something*, and `total_timeout_s` by nothing at all -- it\nexpires while the call is perfectly healthy. Publishing only the first two\nwould describe one build and misdescribe another.\n\n**`null` means this build imposes no bound of that kind**, on the convention\n`server_name_pattern` already sets rather than the null-means-unmeasured rule\nthe nullable measurements follow. It is a statement about THIS BUILD's\nclient: null does not promise a network path, a proxy or a kernel will wait\nforever, only that nothing in the agent gives up.\n\n**Every value is what a caller gets, with no lever to move it.** `McpServer`\ncarries no `timeout` field on any variant, so there is no per-request\noverride; where an agent reads an environment variable for one of these, it\nis the operator's surface and not the caller's, and this service does not set\nit.\n\n**A value is never more generous than the strictest transport in\n`transports`.** One build's idle timeout is six times longer over `stdio`\nthan over `sse`/`http`; the shorter number is the one published, so a client\nplanning against it is never surprised by a transport it did not test."
      },
      "Problem": {
        "properties": {
          "type": {
            "type": "string",
            "title": "Type",
            "default": "about:blank"
          },
          "title": {
            "type": "string",
            "title": "Title"
          },
          "status": {
            "type": "integer",
            "title": "Status"
          },
          "detail": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Detail"
          }
        },
        "type": "object",
        "required": [
          "title",
          "status"
        ],
        "title": "Problem",
        "description": "RFC 7807 problem document."
      },
      "QueryRequest": {
        "properties": {
          "prompt": {
            "type": "string",
            "minLength": 1,
            "title": "Prompt",
            "description": "What to ask the agent -- **the user turn, not a system prompt**. Framing goes in `options.system_prompt`, which on most builds replaces the agent's own.\n\nThe agent also reads configuration from the container's disk, so this is never the whole of what it was told.",
            "examples": [
              "List the files in this directory"
            ]
          },
          "options": {
            "$ref": "#/components/schemas/RunOptions",
            "description": "Per-request overrides. **Omitting a field is not the same as sending its default**: an omitted field takes the server's, which `/v1/capabilities` publishes and which a deployment can change. Send only what you mean to pin.\n\n**An unknown property is a 422 naming the key** -- a typo or a renamed field is reported rather than ignored."
          }
        },
        "type": "object",
        "required": [
          "prompt"
        ],
        "title": "QueryRequest"
      },
      "RunOptions": {
        "properties": {
          "model": {
            "anyOf": [
              {
                "type": "string",
                "minLength": 1
              },
              {
                "type": "null"
              }
            ],
            "title": "Model",
            "description": "Omit to use the server default. An empty string is rejected rather than treated as omission.",
            "examples": [
              "claude-sonnet-5"
            ]
          },
          "resume": {
            "anyOf": [
              {
                "type": "string",
                "minLength": 1
              },
              {
                "type": "null"
              }
            ],
            "title": "Resume",
            "description": "Continue a previous conversation. This is the **SDK** session id (`sdk_session_id`), NOT the service-side `session_id`.\n\nWith a database configured the conversation survives a service restart; without one, only while the CLI's on-disk transcript remains. Resumes a CONVERSATION, not an in-flight stream -- a turn interrupted by a disconnect is not resumable."
          },
          "system_prompt": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "title": "System Prompt",
            "description": "A plain string, or a preset object such as {\"type\": \"preset\", \"preset\": \"claude_code\", \"append\": \"...\"}.\n\n**The string REPLACES the agent's own framing** -- safety rules, tool protocol, workflows. The preset object is the only form that keeps them and appends, and not every build has one: check `unsupported_options` for `{field: system_prompt, types: [\"object\"]}`.\n\n**Not a substitute for ambient configuration**: it does not become a memory file or register a skill, and suppresses nothing on disk -- on one build the workspace's context files are appended after it."
          },
          "allowed_tools": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Allowed Tools",
            "description": "Tools this session may use, in this build's own names -- `default_allowed_tools` lists the published ones.\n\n**An unrecognised name is ignored, never refused.** `Reed` for `Read` grants nothing and reports nothing.\n\nNames come from three places, and only the first can be published: **built-ins** (fixed per build and platform, not per model); **MCP tools** from the servers you send, in that build's spelling (`mcp__<server>__<tool>`); **skills, subagents and plugins** the agent reads from the container's disk.\n\n**Grants permission, not visibility**: an ungranted tool is still offered to the model, attempted, and denied -- which costs a turn. `disallowed_tools` removes it. Not a path or command restriction."
          },
          "disallowed_tools": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Disallowed Tools",
            "description": "Removes a tool outright. Where `allowed_tools` grants permission without hiding anything, this is the field that takes a capability away.\n\n**An unrecognised name is ignored here too**, so a misspelled denial denies nothing -- the more dangerous direction of the same behaviour. Everything in `always_disallowed_tools` is added whatever you send."
          },
          "permission_mode": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Permission Mode",
            "description": "How much the agent may do without asking.\n\n**The ids differ per build** -- read `permission_modes[].id` for this one's, and `.description` for what each does here. An id it does not publish is a 400.\n\n**Not a boundary on any build.** What confines the agent is the container, the sandbox and (on one build) a generated tool policy: see `permission_enforcement` and `sandbox`."
          },
          "effort": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "low",
                  "medium",
                  "high",
                  "xhigh",
                  "max"
                ]
              },
              {
                "type": "null"
              }
            ],
            "title": "Effort",
            "description": "How much reasoning effort to spend.\n\nVocabulary is `effort_levels[]`; **empty means the build has no equivalent and refuses the field with a 400**. A build publishes only the levels it delivers exactly -- one maps a higher level down and omits it rather than letting you believe it was honoured."
          },
          "setting_sources": {
            "anyOf": [
              {
                "items": {
                  "type": "string",
                  "enum": [
                    "user",
                    "project",
                    "local"
                  ]
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Setting Sources",
            "description": "Which ambient on-disk configuration the agent loads. Server default is none; a build with no such concept publishes `[]` and refuses the field.\n\n**A switch over what is already on the container's disk -- never a way to send it.** Memory files, skills, subagents, commands, plugins and settings are read inside the container, and no `RunOptions` field on any build can supply them. This is the only lever, it is coarse, and one build does not have it at all.\n\nTreat that disk as part of the deployment, not part of the request."
          },
          "max_turns": {
            "anyOf": [
              {
                "type": "integer",
                "minimum": 1.0
              },
              {
                "type": "null"
              }
            ],
            "title": "Max Turns",
            "description": "Bound on the agent's turns within one request.\n\nEnforced only where `limits.default_max_turns` and `max_allowed_turns` are published; elsewhere it is a 400 rather than accepted and ignored. **Above the ceiling is a 400, never a clamp** -- a silently shortened budget produces a stop you cannot explain."
          },
          "max_budget_usd": {
            "anyOf": [
              {
                "type": "number",
                "exclusiveMinimum": 0.0
              },
              {
                "type": "null"
              }
            ],
            "title": "Max Budget Usd",
            "description": "**NOT A SPEND CAP. It cannot bound what a session costs.** It is enforced by the CLI against its cumulative figure, and that figure does not move for an INTERRUPTED turn -- so a caller that can interrupt turns can spend without limit under any value set here. The only effective control is an account- or organisation-level budget."
          },
          "timeout_s": {
            "anyOf": [
              {
                "type": "integer",
                "minimum": 1.0
              },
              {
                "type": "null"
              }
            ],
            "title": "Timeout S",
            "description": "Wall clock for this request, in seconds; expiry answers **504**.\n\nEnforced by this service rather than by any agent SDK, so it means the same on every build. The ceiling is `limits.max_allowed_timeout_s` and asking for more is a 400, not a clamp -- **checked at session creation on one build and per turn on another**, so a value accepted at create can still be refused later."
          },
          "working_directory": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Working Directory",
            "description": "Where the agent starts work. Relative paths the agent uses resolve from here, and it decides which project-level configuration the session picks up -- a memory file or a settings file is found relative to this directory.\n\n**A path RELATIVE to `capabilities.workspace_dir`** -- `src/api`, not `/workspace/src/api`. It must stay under the root (`..` is refused) and **must already exist**: the directory is never created from request input.\n\n**Not confinement on most builds** -- the mount and the container are the boundary, and a working directory restricts nothing a tool may reach. On one build the agent's own guard is anchored here, so it also narrows what its file tools can open.\n\n**Renamed from `workspace_subdir`**, which is now an unknown property and therefore a 422 naming the key -- not a silent no-op."
          },
          "include_partial_messages": {
            "type": "boolean",
            "title": "Include Partial Messages",
            "description": "Emit output as it is produced rather than only when a message is complete -- on the SSE routes, token-level deltas instead of one event per message.\n\nAccepted by every build, and **the one option whose default comes from this schema** rather than from server config, so `false` is the answer rather than a placeholder.",
            "default": false
          },
          "include_raw": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Include Raw",
            "description": "Include the full SDK message dump on each event."
          },
          "mcp_servers": {
            "anyOf": [
              {
                "additionalProperties": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/McpStdioServer"
                    },
                    {
                      "$ref": "#/components/schemas/McpSseServer"
                    },
                    {
                      "$ref": "#/components/schemas/McpHttpServer"
                    }
                  ]
                },
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "title": "Mcp Servers",
            "description": "MCP servers for this run, keyed by server name. Omit for none.\n\nThree transports: `stdio` (this service spawns a subprocess -- read `McpStdioServer` first), `sse` and `http` (no subprocess). The SDK's fourth shape, `sdk`, is absent on purpose: it carries a live in-process object and cannot cross an HTTP boundary.\n\n**Refused with 400 when the deployment sets `AGENT_SERVICE_ALLOW_MCP_SERVERS=false`.** Check `Capabilities.allow_mcp_servers` before relying on this field, the same way `credential_sources` is checked before injecting a key.\n\n`env` on a stdio server and `headers` on an http/sse one are the two positions a caller substitutes credentials into. Neither is ever logged by this service.",
            "examples": [
              {
                "acme": {
                  "type": "http",
                  "url": "https://mcp.example.com/mcp"
                }
              }
            ]
          },
          "strict_mcp_config": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Strict Mcp Config",
            "description": "When true, ONLY the servers in `mcp_servers` are used and every other source the CLI would consult is ignored -- project `.mcp.json`, user and global settings, plugin-provided servers.\n\n**This service defaults it to TRUE**, which is not the SDK's default of false. The reason is the workspace: it is mounted from the host and is WRITABLE BY THE AGENT, so a `.mcp.json` sitting in it -- committed by someone else, or written by a previous turn -- would otherwise add servers this caller never asked for and cannot see in its own request. The contract is that what you sent is what runs. Set false to opt into the CLI's own discovery.\n\nNote this service already passes `setting_sources` explicitly (default `[]`). Whether that alone suppresses `.mcp.json` is **not measured** -- this flag does not depend on the answer."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "title": "RunOptions",
        "description": "Per-request overrides. Every field falls back to server config when omitted.\n\n**An unknown property is REJECTED, not ignored** (2026-09-03). It arrives as\nthe framework's own `422` naming the offending key in `loc` with\n`type: extra_forbidden`, which is the same channel a wrong type already uses.\n\nThe reason is this platform's oldest rule pointed at itself: an option\naccepted and never applied is the defect it treats as its worst, and an\nunknown property is that defect in its purest form -- the caller believes\nthey configured something and nothing did. Ignoring it silently is how\n`workspace_subdir` would have survived its own rename."
      },
      "RunResponse": {
        "properties": {
          "session_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Session Id",
            "description": "The SDK's OWN conversation id for this run -- NOT a handle you can put in a `/v1/sessions/{session_id}` path. That path takes `SessionRecord.session_id`, this service's registry handle, which is a different string: feeding this one back is a 404 (measured). The two identifiers have shared a name since Plan 1 and the name is kept as-is because it already ships; `sdk_session_id` below carries the same value under a name that says which id it is, and is what new code should read."
          },
          "sdk_session_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Sdk Session Id",
            "description": "The same value as `session_id` above, under an unambiguous name. Matches `TurnRecord.sdk_session_id` on `SessionRecord.last_turn`, so one name means one identifier across every surface that reports the SDK's id."
          },
          "outcome_recorded": {
            "type": "boolean",
            "title": "Outcome Recorded",
            "description": "False when the run ended without the agent's own terminating result (the agent process crashed, exited early, or was killed). The events list may still be populated, but result, cost and usage are unavailable — this is not the same as a successful run that produced no output.",
            "default": true
          },
          "result": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Result",
            "description": "The agent's final answer as text, reassembled across the turn.\n\n**Null is a real state and not a gap**: a turn that was interrupted, ran out of wall clock, or ended without producing text has no result. That is why `is_error`, `interrupted` and `stop_kind` are separate fields -- do not infer any of them from this one being empty."
          },
          "is_error": {
            "type": "boolean",
            "title": "Is Error",
            "description": "The agent reported failure. A successful HTTP call can carry is_error=true.",
            "default": false
          },
          "interrupted": {
            "type": "boolean",
            "title": "Interrupted",
            "description": "True when this turn was stopped by an explicit interrupt request. The SDK reports an interrupted turn with is_error=true and subtype='error_during_execution' -- identical in shape to a real failure -- so this flag is the only reliable way to tell a deliberate stop from a crash.",
            "default": false
          },
          "stop_kind": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "end_turn",
                  "max_turns",
                  "max_budget",
                  "max_tokens",
                  "refusal",
                  "interrupted",
                  "timed_out",
                  "error",
                  "other"
                ]
              },
              {
                "type": "null"
              }
            ],
            "title": "Stop Kind",
            "description": "WHY THE TURN ENDED, as a closed set every implementation maps onto. Read this rather than the three strings below, which are each an SDK's own vocabulary passed through verbatim and differ between builds for the same ending. Null only when the build cannot tell -- never as a substitute for 'other', which means the build knows the ending and has no mapping for it. Derived from the same facts as `interrupted`, `limit_hit` and `is_error` and guaranteed not to contradict them; where two could apply the order is interrupted, timed out, guardrail, error, then whatever the SDK reported."
          },
          "subtype": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Subtype",
            "description": "The SDK's own terminal subtype, verbatim. Build-specific: do not branch on it, read `stop_kind`."
          },
          "stop_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Stop Reason",
            "description": "The SDK's own stop reason, verbatim. Build-specific: do not branch on it, read `stop_kind`."
          },
          "terminal_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Terminal Reason",
            "description": "The SDK's own free-text description of the ending, verbatim -- an error message where there was one. For a human reading a log, not for a client to match on."
          },
          "limit_hit": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "turns",
                  "budget"
                ]
              },
              {
                "type": "null"
              }
            ],
            "title": "Limit Hit",
            "description": "Which guardrail ended the run, if any. Never 'timeout': a timed-out run raises RunTimeout, which surfaces as a 504 Problem document (/v1/query) or an in-band event: error (/v1/query/stream), not a RunResponse -- so this field would never actually carry that value. Kept beside `stop_kind`, which reports the same two endings as 'max_turns' and 'max_budget'."
          },
          "num_turns": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Num Turns",
            "description": "Agent turns inside this one request, as the agent counted them.\n\n**Null when the agent produced no result envelope** -- an interrupted or timed-out turn reports neither this nor `duration_ms`, which is a different thing from reporting zero. The unit is the agent's own, so it is a number to display rather than one to compare across builds."
          },
          "total_cost_usd": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Total Cost Usd",
            "description": "Cost in USD from the SDK. **Its scope depends on the endpoint.** On `POST /v1/query` it is that run's cost. On a session turn it is the CUMULATIVE cost of every turn so far, growing monotonically -- not the cost of the turn just taken. Read `turn_cost_usd` for that.\n\n`null` when the SDK reported a successful turn it attributed no cost to. Missing rather than zero, because such a turn did run."
          },
          "turn_cost_usd": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Turn Cost Usd",
            "description": "What THIS turn cost, in USD. On a session turn it is the difference against the previous turn; on `POST /v1/query` it equals `total_cost_usd`.\n\n**`null` means nobody can say**, and it is not 0.0: the run produced no result, the result carried no price, or the turn was interrupted, **or the agent reports no monetary figure at all** -- on such a build every cost field is null forever, which is not the same as free. `0.0` is reserved for a turn that genuinely cost nothing."
          },
          "duration_ms": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Duration Ms",
            "description": "How long the turn took, in milliseconds, as the agent measured it.\n\n**The agent's clock, not this service's**, so it excludes whatever the HTTP layer spent around it and is null when no result envelope arrived. To bound a request use `RunOptions.timeout_s`, which this service enforces itself."
          },
          "usage": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "title": "Usage",
            "description": "Token counts for THIS run, **passed through from the underlying agent verbatim and unnormalised**. The keys, and whether a key means what its name suggests, are that agent's -- one build's counter for tool calls is measured to report zero on a turn that made one.\n\n**Zeros mean the agent reported nothing, not that nothing happened**: a turn that failed or was interrupted still consumed tokens on every build measured.\n\nRead `token_usage` instead for figures with a meaning the specification defines, and read this only when you know which implementation answered."
          },
          "token_usage": {
            "$ref": "#/components/schemas/TokenUsage",
            "description": "The specification's own named token counts for this run, filled by every implementation. **Always present**; each field is nullable, and `null` means not reported rather than zero. `usage` above stays a verbatim SDK pass-through -- this is the shape a consumer reads without knowing which implementation answered."
          },
          "model_usage": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "title": "Model Usage",
            "description": "Per-model usage, when the agent reports any. **A map, or null on a build whose agent reports no per-model breakdown at all.**\n\n**ITS SCOPE IS PER BUILD AND YOU MUST NOT ASSUME EITHER ANSWER.** On one implementation this is CUMULATIVE for the connection, so summing across turns multiplies the real figure; on another it is PER TURN, so summing is the correct thing to do. The two are indistinguishable from the payload. **Read the implementation's own guide before aggregating it**, and prefer `token_usage`, whose scope the specification fixes.\n\nA build may also report more models than the one requested -- an internal router or summariser -- so a key here is not evidence that the caller selected that model."
          },
          "permission_denials": {
            "anyOf": [
              {
                "items": {},
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Permission Denials",
            "description": "Tools the agent tried and was refused, as it reported them.\n\n**Where the cost of `allowed_tools` shows up**: an ungranted tool is still offered to the model, attempted, and denied -- a spent turn, recorded here. Shape is the agent's own; **null means the build reports no such list**, not that nothing was denied."
          },
          "events": {
            "items": {
              "$ref": "#/components/schemas/AgentEvent"
            },
            "type": "array",
            "title": "Events",
            "description": "Every event of the turn, in `seq` order. **The streaming routes deliver the same objects one at a time instead.**\n\nShaped by `include_partial_messages` (deltas or whole messages) and `include_raw` (whether each event carries the agent's own payload)."
          }
        },
        "type": "object",
        "title": "RunResponse"
      },
      "Sandbox": {
        "properties": {
          "network_access": {
            "type": "boolean",
            "title": "Network Access",
            "description": "Whether the agent's own shell commands can reach the network.\n\n**`false` means a tool cannot open a socket at all** -- no package install, no `curl`, no API call from a command. It is not a statement about this service, which reaches the model API either way, and not about the container, which may well have egress.\n\n**`true` is not a weaker service, it is a different boundary.** A build reporting `true` may be relying on the container and its network policy for confinement, which is a deployment's decision rather than a defect -- read it as *the agent is not stopped here* rather than as *this build is unsafe*.",
            "examples": [
              false
            ]
          },
          "confines_writes_to_workspace": {
            "type": "boolean",
            "title": "Confines Writes To Workspace",
            "description": "Whether the agent's writes are confined to the workspace by something inside the container.\n\n**`false` does not mean the agent may write anywhere a caller cares about** -- the container's mount layout is still the boundary, and on a build with no in-container sandbox that is the only one. It means no second mechanism is enforcing it, so a process that escapes the intended paths is not stopped again on its way out.",
            "examples": [
              true
            ]
          }
        },
        "type": "object",
        "required": [
          "network_access",
          "confines_writes_to_workspace"
        ],
        "title": "Sandbox",
        "description": "What the agent's own tools are confined by, beyond the container.\n\n**AS-32, and it is the difference a tool-using Agent notices first.** The\ntwo implementations are not comparable here and nothing published said so:\none runs its shell inside a sandbox that blocks egress, the other runs it\nunconfined and relies on the container being the boundary. **The same Agent\n-- one whose capability is *install this package* or *call this API from a\nshell command* -- works on one build and fails on the other.**\n\n**This describes the AGENT's tools, not the service.** The service reaches\nthe model API regardless; what is confined is what the agent can do with a\nshell."
      },
      "Sdk": {
        "properties": {
          "name": {
            "type": "string",
            "title": "Name",
            "description": "The distribution name of the agent SDK this build wraps, e.g. `claude-agent-sdk`. **This is the axis on which implementations differ** -- a Codex or Gemini build is a different service image reporting a different name here, not this image in another mode.",
            "examples": [
              "claude-agent-sdk"
            ]
          },
          "version": {
            "type": "string",
            "title": "Version",
            "description": "The installed version of that SDK. Same value as the deprecated top-level `sdk_version`, read from the same place.",
            "examples": [
              "0.2.128"
            ]
          }
        },
        "type": "object",
        "required": [
          "name",
          "version"
        ],
        "title": "Sdk",
        "description": "The agent SDK this build wraps, and its version."
      },
      "Service": {
        "properties": {
          "spec": {
            "$ref": "#/components/schemas/Spec",
            "description": "Which interface document this build implements. Two implementations satisfying the same document report the same value here."
          },
          "impl": {
            "$ref": "#/components/schemas/Impl",
            "description": "Which build this is. Read this, not `info.version`."
          },
          "sdk": {
            "$ref": "#/components/schemas/Sdk",
            "description": "Which agent SDK this build wraps, and its version. Supersedes `sdk_version`, which cannot say WHICH SDK."
          },
          "sdk_version": {
            "type": "string",
            "title": "Sdk Version",
            "description": "**Deprecated: read `sdk.version` instead.** Still emitted and still correct -- the same value from the same source.",
            "deprecated": true
          }
        },
        "type": "object",
        "required": [
          "spec",
          "impl",
          "sdk",
          "sdk_version"
        ],
        "title": "Service",
        "description": "Who is answering: the contract, the build, and the agent underneath.\n\n**Read `impl.version` to decide whether to re-read this payload**, and never\nas a cache key on its own -- `config` moves with the deployment rather than\nwith the build."
      },
      "SessionCreate": {
        "properties": {
          "options": {
            "$ref": "#/components/schemas/RunOptions"
          },
          "title": {
            "anyOf": [
              {
                "type": "string",
                "maxLength": 200
              },
              {
                "type": "null"
              }
            ],
            "title": "Title"
          },
          "sdk_session_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Sdk Session Id",
            "description": "**The SDK conversation id to use**, assigned by the caller instead of by the CLI. Must be a UUID, and is returned on the 201 so the mapping exists before the first model call.\n\nNOT the service-side `session_id`, which is minted here and returned as `SessionRecord.session_id`.\n\n**Rejected with 400 alongside `options.resume`** -- the CLI refuses that combination.\n\n**Must be unused by a TURN.** An id whose conversation has already taken a turn in the same workspace is refused. Creation alone does not consume it: a create that timed out, or that succeeded and was closed without a turn, leaves the id reusable.\n\n**Retrying a create that timed out:** reconcile with `GET /v1/sessions` matching on `sdk_session_id`. If a session holds your id the create succeeded and you lost the response; if none does, retry with the SAME id.",
            "examples": [
              "7ad25f07-08d4-4b3a-9f21-2b6a1c7d3e55"
            ]
          },
          "session_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Session Id",
            "description": "**Deprecated alias for `sdk_session_id`. Use that instead.**\n\nSending both is accepted only when they are equal; otherwise the request is rejected rather than one being silently preferred.",
            "deprecated": true
          }
        },
        "type": "object",
        "title": "SessionCreate",
        "description": "Create a session. Options are fixed for the session's lifetime except\nfor model and permission_mode, which PATCH can change."
      },
      "SessionList": {
        "properties": {
          "sessions": {
            "items": {
              "$ref": "#/components/schemas/SessionRecord"
            },
            "type": "array",
            "title": "Sessions"
          }
        },
        "type": "object",
        "title": "SessionList"
      },
      "SessionMode": {
        "properties": {
          "id": {
            "type": "string",
            "minLength": 1,
            "title": "Id",
            "description": "What a caller sends as `options.permission_mode`. Stable for a given build; `default` and `plan` are well-known across builds that have an equivalent.",
            "examples": [
              "plan"
            ]
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "title": "Name",
            "description": "Short human label. Never branch on it.",
            "examples": [
              "Plan"
            ]
          },
          "description": {
            "type": "string",
            "minLength": 1,
            "title": "Description",
            "description": "What this mode actually permits ON THIS BUILD -- not what the id suggests elsewhere. Two builds may honour the same id by different mechanisms, and this is where that is said.",
            "examples": [
              "Read and reason; change nothing."
            ]
          }
        },
        "type": "object",
        "required": [
          "id",
          "name",
          "description"
        ],
        "title": "SessionMode",
        "description": "One permission mode a build actually honours.\n\n**The id is the contract; the name and description are for a human.** A\nclient branches on `id` and never on the prose, which is the same rule\n`unsupported_options` learned when a sentence appeared where an identifier\nwas expected and no client could match it."
      },
      "SessionRecord": {
        "properties": {
          "session_id": {
            "type": "string",
            "title": "Session Id"
          },
          "sdk_session_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Sdk Session Id",
            "description": "The SDK's conversation id for this session, or null when it is not known yet. Populated at creation when the caller supplied `session_id` on `POST /v1/sessions`; otherwise the CLI mints it at the first turn and this stays null until then (measured: the id does not exist before the first turn). Distinct from `session_id` above, which is this service's registry handle and the value used in `/v1/sessions/{sid}` paths."
          },
          "agent_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Agent Id",
            "description": "The `AGENT_ID` of the container that created this session, or `null` when it was started without one.\n\n**Provenance, not authorisation.** This service neither parses it nor enforces anything with it. It exists because a caller may run many agents against ONE shared database, where every row belongs to somebody and nothing said which -- so a session the caller's own bookkeeping missed is not untidy, it is one nobody can be shown to own.\n\n**Nullable but not optional**: the field is always present, so `null` means \"that container had no `AGENT_ID`\" and can never mean \"this service did not tell you\". Same rule as `Health.database_usable`.\n\n**It cannot be set by a caller.** The value is a process constant read from the environment at startup; there is no request field for it to arrive through, which is structural rather than validated. `POST /v1/sessions` accepts no `agent_id`.",
            "examples": [
              "agent-7f3c9a1e"
            ]
          },
          "title": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Title"
          },
          "status": {
            "type": "string",
            "enum": [
              "idle",
              "running",
              "closed"
            ],
            "title": "Status"
          },
          "created_at": {
            "type": "number",
            "title": "Created At"
          },
          "last_used_at": {
            "type": "number",
            "title": "Last Used At"
          },
          "turns": {
            "type": "integer",
            "title": "Turns",
            "description": "Turns that reached a result. A turn that timed out, failed or was abandoned mid-drain is not counted; one whose result was produced but never delivered (the client hung up on the last frame) is."
          },
          "total_cost_usd": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Total Cost Usd",
            "description": "The SDK's running total for this session's connection, assigned verbatim and never summed. **A floor on what the session cost, not the figure** -- an interrupted turn does not move it, and a turn the SDK attributed no cost to leaves it where it was. Read it as \"at least this much\".\n\n**`null` means this build cannot price a turn at all**, which is not the same as `0.0`. Some agent SDKs report token usage and no monetary figure; such an implementation reports `null` here for every session rather than a `0.0` that would read as free. Always present, so `null` can never mean \"not told\"."
          },
          "model": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Model",
            "description": "The model this session is CURRENTLY configured with -- resolved at creation from the request or the server default, and updated by a successful PATCH. This is the read-back for PATCH: without it there is no way to confirm a model change took."
          },
          "permission_mode": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Permission Mode",
            "description": "The permission mode this session is currently configured with, resolved and updated exactly like `model`. A plain string rather than the `PermissionMode` enum on purpose: the server default is an unconstrained environment setting, and validating it here would turn an operator's config typo into a 500 on the one endpoint whose job is to report what a session looks like. \n\nOnly these two fields are echoed. Every other option is fixed for the session's lifetime, and `system_prompt` in particular carries caller-supplied content plus the server's own workspace layout -- not something to return from a list endpoint."
          },
          "last_residue_discarded": {
            "type": "integer",
            "title": "Last Residue Discarded",
            "description": "How many stale messages the LAST turn discarded before it started. Non-zero means a previous turn was abandoned with messages still in flight on the SDK's connection-scoped buffer (spike case S3): they would otherwise have been read as this turn's own -- one caller's turn attributed to another -- so they were dropped. Reset at the top of every turn, so it always describes the most recent one and never a stale count from an older abnormal turn.",
            "default": 0
          },
          "last_turn": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TurnRecord"
              },
              {
                "type": "null"
              }
            ],
            "description": "What became of the most recent turn. NULL means this session has never taken one -- not that the last turn reported nothing. This is how a caller whose stream dropped asks what happened to the turn it lost: `turns` and `total_cost_usd` do not move for an abandoned turn, so without this a session that lost a turn looks exactly like one that never took any."
          },
          "context_usage": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ContextUsage"
              },
              {
                "type": "null"
              }
            ],
            "description": "Populated on the detail endpoint only."
          }
        },
        "type": "object",
        "required": [
          "session_id",
          "status",
          "created_at",
          "last_used_at",
          "turns",
          "total_cost_usd"
        ],
        "title": "SessionRecord"
      },
      "SessionUpdate": {
        "properties": {
          "model": {
            "anyOf": [
              {
                "type": "string",
                "minLength": 1
              },
              {
                "type": "null"
              }
            ],
            "title": "Model",
            "description": "An empty string is rejected rather than forwarded (follow-up item 9). Unlike the two permissive behaviours above, forwarding it is not harmless: it issues a real `set_model` control request to the agent with a meaningless argument."
          },
          "permission_mode": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Permission Mode"
          }
        },
        "type": "object",
        "title": "SessionUpdate",
        "description": "Both fields optional; omitted fields are left unchanged.\n\nTwo other permissive behaviours are DELIBERATE and left as they are\n(follow-up item 9): `{\"bogus\": 1}` is accepted, because no model in this\nmodule sets `extra=\"forbid\"`, and `{\"model\": null}` is indistinguishable\nfrom omitting the field, because pydantic gives both the same default --\nso a caller cannot reset a session to the server default. Both are\nproperties of how this whole schema module behaves rather than of this\nmodel, and changing either here alone would make one model inconsistent\nwith every other. Pinned in tests/test_session_schemas.py so they are a\nrecorded decision, not an accident."
      },
      "Spec": {
        "properties": {
          "document_version": {
            "type": "string",
            "title": "Document Version",
            "description": "The version of the interface document this build implements. Also what `GET /openapi.json` reports as `info.version`, and what names the published `openapi-<version>.json`.\n\n**Not this build's version** -- read `impl.version` for that. Two implementations in different languages serve the same document and report the same value here.",
            "examples": [
              "0.16.0"
            ]
          }
        },
        "type": "object",
        "required": [
          "document_version"
        ],
        "title": "Spec",
        "description": "The interface document this build implements."
      },
      "StoredEvent": {
        "properties": {
          "id": {
            "type": "integer",
            "title": "Id",
            "description": "Storage sequence. Stable, and usable as a pagination cursor."
          },
          "run_id": {
            "type": "string",
            "title": "Run Id"
          },
          "seq": {
            "type": "integer",
            "title": "Seq",
            "description": "Position within its own run, as the driver counted it."
          },
          "at": {
            "type": "string",
            "format": "date-time",
            "title": "At"
          },
          "type": {
            "type": "string",
            "title": "Type"
          },
          "subtype": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Subtype"
          },
          "content": {
            "anyOf": [
              {},
              {
                "type": "null"
              }
            ],
            "title": "Content"
          }
        },
        "type": "object",
        "required": [
          "id",
          "run_id",
          "seq",
          "at",
          "type"
        ],
        "title": "StoredEvent",
        "description": "One recorded SDK message."
      },
      "StoredRun": {
        "properties": {
          "run_id": {
            "type": "string",
            "title": "Run Id"
          },
          "session_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Session Id",
            "description": "The service-side session id, or null for a one-shot /v1/query run."
          },
          "sdk_session_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Sdk Session Id"
          },
          "started_at": {
            "type": "string",
            "format": "date-time",
            "title": "Started At"
          },
          "finished_at": {
            "anyOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ],
            "title": "Finished At"
          },
          "prompt": {
            "type": "string",
            "title": "Prompt"
          },
          "result_text": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Result Text"
          },
          "result_subtype": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Result Subtype"
          },
          "stop_kind": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "end_turn",
                  "max_turns",
                  "max_budget",
                  "max_tokens",
                  "refusal",
                  "interrupted",
                  "timed_out",
                  "error",
                  "other"
                ]
              },
              {
                "type": "null"
              }
            ],
            "title": "Stop Kind",
            "description": "WHY THE TURN ENDED, as a closed set every implementation maps onto -- read this rather than the SDK-specific strings beside it. Null only when the build cannot tell; 'other' when it knows and has no mapping. Never contradicts `interrupted`, `timed_out`, `limit_hit` or `is_error`."
          },
          "stop_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Stop Reason"
          },
          "terminal_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Terminal Reason"
          },
          "limit_hit": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Limit Hit"
          },
          "num_turns": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Num Turns"
          },
          "duration_ms": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Duration Ms"
          },
          "duration_api_ms": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Duration Api Ms"
          },
          "cost_usd": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Cost Usd",
            "description": "What this run cost. `null` means nobody can say -- NEVER 0.0, which would claim the run was free. An interrupted turn is unattributed, not free."
          },
          "usage": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "title": "Usage"
          },
          "model_usage": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "title": "Model Usage"
          },
          "permission_denials": {
            "anyOf": [
              {},
              {
                "type": "null"
              }
            ],
            "title": "Permission Denials"
          },
          "errors": {
            "anyOf": [
              {},
              {
                "type": "null"
              }
            ],
            "title": "Errors"
          },
          "api_error_status": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Api Error Status"
          },
          "is_error": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Is Error",
            "description": "The AGENT reported its task failed. Distinct from a machinery failure."
          },
          "interrupted": {
            "type": "boolean",
            "title": "Interrupted",
            "default": false
          },
          "timed_out": {
            "type": "boolean",
            "title": "Timed Out",
            "default": false
          },
          "outcome_missing": {
            "type": "boolean",
            "title": "Outcome Missing",
            "description": "The run never produced the agent's own terminating result: a crash, a killed process, an abandoned consumer, or a timeout. A real state, distinct from a clean finish.",
            "default": false
          }
        },
        "type": "object",
        "required": [
          "run_id",
          "started_at",
          "prompt"
        ],
        "title": "StoredRun",
        "description": "One recorded run or turn, as stored.\n\nWider than `RunResponse`: it carries `duration_api_ms`, `errors` and\n`api_error_status`, which are diagnostic detail a stored row wants and a\nstreaming caller does not."
      },
      "TokenUsage": {
        "properties": {
          "input_tokens": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Input Tokens",
            "description": "Prompt tokens for this turn. `null` if not reported."
          },
          "output_tokens": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Output Tokens",
            "description": "Generated tokens for this turn. `null` if not reported."
          },
          "cache_read_tokens": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Cache Read Tokens",
            "description": "Prompt tokens served from cache -- the cheap half of the cache pair. `null` if not reported."
          },
          "cache_write_tokens": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Cache Write Tokens",
            "description": "Prompt tokens written to cache -- the **expensive** half, typically billed at a premium.\n\n**`null` on an implementation whose SDK has no such counter**, which is not the same as zero: on such a build a cache write is a charge you cannot see from this API. It is reported as `null` rather than omitted precisely so that gap is visible."
          },
          "reasoning_output_tokens": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reasoning Output Tokens",
            "description": "Generated reasoning tokens, where the SDK reports them separately from `output_tokens`. `null` on an implementation that does not distinguish them -- which does not mean none were generated."
          }
        },
        "type": "object",
        "title": "TokenUsage",
        "description": "Named token counts for one turn, filled by every implementation.\n\n**0.19.0, and it exists because `usage` cannot serve this purpose.** `usage`\nis a verbatim SDK pass-through, so its keys are whichever SDK produced it --\n`cache_creation_input_tokens` on one build, `cachedInputTokens` on another.\nA consumer wanting \"how many tokens did this turn cost\" had to write one\nscanner per implementation and update it whenever a third appeared. This\nobject is the specification's own spelling, and `usage` stays untouched\nbeside it so the raw numbers remain reachable when the two disagree.\n\n**Every field is a nullable integer, and `null` means NOT REPORTED -- never\nzero.** The distinction is not pedantry: a build that cannot count cache\nwrites reporting `0` would show a 1.25x charge as free, which is exactly the\nmisleading total this object was asked for to prevent. Same rule as\n`total_cost_usd` since 0.16.0 and `Health.database_usable`.\n\n**The object itself is always present.** A `null` object could not be told\napart from \"this implementation does not report token counts\", and that\nambiguity is what AS-17a rejects.\n\nScope is THIS run, matching `usage` and not `model_usage`."
      },
      "TranscriptPage": {
        "properties": {
          "session_id": {
            "type": "string",
            "title": "Session Id"
          },
          "events": {
            "items": {
              "$ref": "#/components/schemas/StoredEvent"
            },
            "type": "array",
            "title": "Events"
          },
          "next_after": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next After",
            "description": "Pass as `after` to fetch the next page. `null` means this is the last page -- NOT that the session has ended, which is `status` on the record."
          }
        },
        "type": "object",
        "required": [
          "session_id",
          "events"
        ],
        "title": "TranscriptPage",
        "description": "One page of a session's transcript, oldest first."
      },
      "TurnRecord": {
        "properties": {
          "sdk_session_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Sdk Session Id",
            "description": "The SDK's OWN conversation id for this turn -- what the CLI reports on its init and result messages. Named `sdk_session_id`, never a bare `session_id`, because that name already denotes the registry handle on `SessionRecord`: the two are different identifiers and `POST /v1/sessions/{this value}/messages` is a 404. Measured."
          },
          "outcome_recorded": {
            "type": "boolean",
            "title": "Outcome Recorded",
            "description": "False when the turn ended without a terminating ResultMessage -- abandoned mid-drain (the usual result of a dropped SSE connection), timed out, or the agent process died. Everything below except `interrupted` and `timed_out` is then null or false because the SDK never reported it, not because it was measured to be so.",
            "default": false
          },
          "interrupted": {
            "type": "boolean",
            "title": "Interrupted",
            "description": "This turn was stopped by an explicit interrupt request. The SDK reports an interrupted turn identically to a crash (is_error=true, subtype='error_during_execution'), so this flag is the only way to tell them apart.",
            "default": false
          },
          "timed_out": {
            "type": "boolean",
            "title": "Timed Out",
            "description": "This turn exceeded its time budget and was force-ended. Present HERE but deliberately absent from `RunResponse`: a turn that times out on the wire is a 504 and the status code says so, but on a record fetched afterwards the status code is long gone -- possibly to a different caller entirely -- and a timeout is otherwise indistinguishable from any other `outcome_recorded: false` ending.",
            "default": false
          },
          "is_error": {
            "type": "boolean",
            "title": "Is Error",
            "description": "The agent reported failure. See `interrupted`.",
            "default": false
          },
          "subtype": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Subtype"
          },
          "stop_kind": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "end_turn",
                  "max_turns",
                  "max_budget",
                  "max_tokens",
                  "refusal",
                  "interrupted",
                  "timed_out",
                  "error",
                  "other"
                ]
              },
              {
                "type": "null"
              }
            ],
            "title": "Stop Kind",
            "description": "WHY THE TURN ENDED, as a closed set every implementation maps onto -- read this rather than the SDK-specific strings beside it. Null only when the build cannot tell; 'other' when it knows and has no mapping. Never contradicts `interrupted`, `timed_out`, `limit_hit` or `is_error`."
          },
          "stop_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Stop Reason"
          },
          "terminal_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Terminal Reason"
          },
          "limit_hit": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "turns",
                  "budget"
                ]
              },
              {
                "type": "null"
              }
            ],
            "title": "Limit Hit"
          },
          "num_turns": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Num Turns"
          },
          "duration_ms": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Duration Ms"
          },
          "turn_cost_usd": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Turn Cost Usd",
            "description": "What this turn cost, in USD: the SDK's cumulative figure for it minus the previous turn's. Null means nobody can say (no ResultMessage, one carrying no price, an ABORTED turn whose cumulative did not move, or a SUCCESSFUL turn the SDK attributed nothing to), which is not 0.0. See `RunResponse.turn_cost_usd`."
          }
        },
        "type": "object",
        "title": "TurnRecord",
        "description": "What became of a session's most recent turn.\n\nA NESTED model rather than flat `last_turn_*` fields on `SessionRecord`,\nfor one reason that decides it: a session that has never taken a turn must\nbe able to say so unambiguously. Nested, that is `last_turn: null`. Flat,\nthe only available answer is every field being falsy at once -- and\n`last_turn_interrupted: false` is a claim about a turn that does not\nexist, indistinguishable from a real turn nobody interrupted.\n\nDeliberately NOT a `RunResponse`. This is a record fetched later, so it\ncarries no `events` (already streamed, and unbounded) and no `result`\ntext: \"what became of the turn\" is answered by the flags below, while the\noutput belongs to whoever took the turn. `GET /v1/sessions` returns a list\nof these records, and the model's prose output is not something to hand\nout by the pageful to anyone enumerating sessions."
      },
      "TurnRequest": {
        "properties": {
          "prompt": {
            "type": "string",
            "minLength": 1,
            "title": "Prompt",
            "examples": [
              "What did we just look at?"
            ]
          }
        },
        "type": "object",
        "required": [
          "prompt"
        ],
        "title": "TurnRequest"
      },
      "UnsupportedOption": {
        "properties": {
          "field": {
            "type": "string",
            "title": "Field",
            "description": "The `RunOptions` property name, exactly as it appears in a request. **An identifier and never prose**, so a client can compare it directly against the key it was about to send.",
            "examples": [
              "max_turns"
            ]
          },
          "types": {
            "anyOf": [
              {
                "items": {
                  "type": "string",
                  "enum": [
                    "string",
                    "number",
                    "boolean",
                    "object",
                    "array"
                  ]
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Types",
            "description": "**Absent means the field is refused whatever it contains.** Present means only those JSON types are refused and the others are honoured -- `system_prompt` with `[\"object\"]` on a build that takes the string form and has no equivalent for the preset object.\n\nTypes are named as JSON names the request itself uses, so the check is `types is null || types.contains(typeof value)` and needs no knowledge of any SDK.\n\n**An absent key and an explicit `null` mean the same thing**, and a reader must accept both. This service always emits the key -- presence with `null` is the convention the whole document follows, for the reason `Health.database_usable` gives: an absent field cannot be told apart from one a build is too old to send.",
            "examples": [
              [
                "object"
              ],
              null
            ]
          },
          "values": {
            "anyOf": [
              {
                "items": {},
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Values",
            "description": "**Absent means the field is refused whatever it contains** -- the same rule `types` follows, so every entry written before this key existed keeps the meaning it had. Present means only these VALUES are refused and every other value of the field is honoured.\n\n**The case it exists for**: a build that implements `strict_mcp_config` and can only ever run strict refuses `false` and honours `true`. The field takes one JSON type, so `types` cannot say that, and naming the field alone would promise a 400 for the value that works.\n\n**Read this beside the matching capability, never instead of it.** `Capabilities.strict_mcp_config` is the server-side DEFAULT and says nothing about which values are accepted; this says which are refused. Two different questions.\n\nValues are compared as JSON, so a reader needs no knowledge of any SDK. **An absent key and an explicit `null` mean the same thing**, on the convention the whole document follows.",
            "examples": [
              [
                false
              ],
              null
            ]
          }
        },
        "type": "object",
        "required": [
          "field"
        ],
        "title": "UnsupportedOption",
        "description": "One `RunOptions` field a build refuses, and optionally only in some shapes.\n\n**This was a bare string until Agent Studio pointed out that it could not be\nacted on** (2026-08-09). Six of seven entries were field names and the\nseventh was `\"system_prompt (preset form)\"`, so the obvious client --\n`if (unsupported_options.contains(fieldName))` -- never matched\n`system_prompt`. Publishing a difference in a form nobody can branch on is\nprecisely what AS-32 exists to prevent, so the entry became a structure\nrather than a sentence.\n\n**`values` is the same defect found a second time** (Agent Harness,\n2026-08-12). A build refuses `strict_mcp_config: false` and honours `true`;\nthe field takes one JSON type, so `types` cannot express it and listing the\nfield alone would promise a 400 for the value that works. It was therefore\nlisted nowhere and was invisible to the one mechanism built for exactly this\n-- which the consumer found by asking rather than by being told.\n\nThe three keys are read together, and each null is *no constraint of that\nkind*::\n\n    refused = field matches\n              && (types  is null || types.contains(jsonTypeOf(v)))\n              && (values is null || values.contains(v))"
      },
      "ValidationError": {
        "properties": {
          "loc": {
            "items": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ]
            },
            "type": "array",
            "title": "Location"
          },
          "msg": {
            "type": "string",
            "title": "Message"
          },
          "type": {
            "type": "string",
            "title": "Error Type"
          },
          "input": {
            "title": "Input"
          },
          "ctx": {
            "type": "object",
            "title": "Context"
          }
        },
        "type": "object",
        "required": [
          "loc",
          "msg",
          "type"
        ],
        "title": "ValidationError"
      },
      "ValidationFailure": {
        "description": "One thing wrong with a request body, and **never the value that was wrong.**\n\n`loc` is what a client acts on: it names the field. `input` -- which the\nframework's own error carries -- is deliberately absent, and that is the\nwhole reason this model exists rather than the framework's.",
        "properties": {
          "loc": {
            "description": "The path to the offending field, framework order: `[\"body\", \"options\", \"permission_mode\"]`. **This is the field a client needs** -- a 422 that says only *something was wrong* makes the caller diff their own request against the document.",
            "examples": [
              [
                "body",
                "options",
                "permission_mode"
              ]
            ],
            "items": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ]
            },
            "title": "Loc",
            "type": "array"
          },
          "msg": {
            "description": "Prose, and it may change. Do not branch on it.",
            "examples": [
              "Input should be a valid string"
            ],
            "title": "Msg",
            "type": "string"
          },
          "type": {
            "description": "The framework's machine-readable failure kind -- `string_type`, `missing`, `model_attributes_type`. Stable enough to branch on, and it is what to branch on rather than `msg`.",
            "examples": [
              "string_type"
            ],
            "title": "Type",
            "type": "string"
          }
        },
        "required": [
          "loc",
          "msg",
          "type"
        ],
        "title": "ValidationFailure",
        "type": "object"
      },
      "ValidationProblem": {
        "description": "A 422, as an RFC 7807 problem document that names the fields.\n\n**This replaced the framework's `HTTPValidationError` on 2026-08-19**, and\nthe interesting part is that three artifacts disagreed before it: two builds\nRETURNED the framework's shape, all three DECLARED it, and all three guides\nsaid errors -- *including a 422 from validation* -- are problem documents.\nSo the documents and the guides contradicted each other and one build\ncontradicted its own document.\n\n**Why not simply conform to what was declared.** The framework's error\ncarries `input`: the offending value, echoed back. A malformed body can\ncontain a caller's own MCP bearer token, this service is unauthenticated by\ndefault, and an error body is the thing most likely to be logged by whatever\nsits in front. One build refused to echo it for exactly that reason and was\nright to; what it got wrong was doing so while its document promised\notherwise, and withholding `loc` along with `input`.\n\n**So this carries `loc`, `msg` and `type`, and never `input`.** A client\nlearns which field was wrong, which is what a client needs, and the value\nstays out of the response.",
        "properties": {
          "type": {
            "default": "about:blank",
            "title": "Type",
            "type": "string"
          },
          "title": {
            "title": "Title",
            "type": "string"
          },
          "status": {
            "title": "Status",
            "type": "integer"
          },
          "detail": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Detail"
          },
          "errors": {
            "description": "Every failure in the request, not just the first. Empty is not expected: a 422 with nothing here would say less than the status already says.",
            "items": {
              "$ref": "#/components/schemas/ValidationFailure"
            },
            "title": "Errors",
            "type": "array"
          }
        },
        "required": [
          "title",
          "status",
          "errors"
        ],
        "title": "ValidationProblem",
        "type": "object"
      },
      "PrebootSpec": {
        "title": "PrebootSpec",
        "description": "This build's pre-boot facts. **Not an HTTP surface** -- every field is needed before a container exists to be asked, which is why it appears here as a component rather than as a response: the environment the container is created with, a certificate written between create and start, and which database it may be pointed at. Read the image's `com.npf.agent-service.impl` and `.document-version` labels to find this document; nothing has to be executed. Values are pinned with `const` where they belong to the build; `version` and `impl.version` move on the implementation stream and are left open.",
        "type": "object",
        "additionalProperties": false,
        "required": [
          "version",
          "document_version",
          "schema_revision",
          "impl",
          "credential_sources",
          "model_api",
          "provider_selectors",
          "auth_enforced",
          "endpoint_source",
          "ca_bundle_source",
          "listen",
          "runs_as"
        ],
        "properties": {
          "version": {
            "type": [
              "string",
              "null"
            ],
            "minLength": 1,
            "description": "This build's own version. Moves on the implementation stream, so it is NOT pinned here -- read it from the image. Equal to `impl.version`."
          },
          "document_version": {
            "type": "string",
            "minLength": 1,
            "description": "The OpenAPI document this image serves -- this document. A `-snapshot` suffix means it can change under you.",
            "const": "0.21.0"
          },
          "schema_revision": {
            "type": "string",
            "minLength": 1,
            "description": "The Alembic revision this image requires of its database; a boot gate refuses any other and the container exits 3. Pinned here because the database is chosen before the container is created, and starting one to read the refusal is a late and expensive way to ask.",
            "const": "d3f9a0c15e27"
          },
          "impl": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "name",
              "version"
            ],
            "properties": {
              "name": {
                "type": "string",
                "minLength": 1,
                "description": "The build. Same value as `deployment.service.impl.name`, and what a per-implementation table keys on.",
                "const": "gemini-python"
              },
              "version": {
                "type": [
                  "string",
                  "null"
                ],
                "minLength": 1,
                "description": "Equal to the top-level `version`, and open for the same reason."
              }
            },
            "description": "The image's own statement of which build it is. The two substitutes are both worse: an image tag is a string an operator typed and a configured provider is a field an operator chose, so either can disagree with what is actually running."
          },
          "credential_sources": {
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1
            },
            "minItems": 1,
            "description": "Environment variables carrying a model credential for this build. Validate the variable you inject against this list before starting a container. Never empty: a build no credential could satisfy could not take a turn.",
            "const": [
              "GEMINI_API_KEY"
            ]
          },
          "model_api": {
            "type": "string",
            "minLength": 1,
            "description": "The target family this build drives, reached through this build's own `credential_sources` and `endpoint_source`. A consumer relaying to a vendor carries one mapping: `claude` is the Anthropic API, `codex` the OpenAI API, `gemini` the Gemini API. **Not a restatement of `impl.name`**, which carries the implementation language -- a second build driving the same target in another language publishes the same `model_api` and a different `impl.name`.",
            "const": "gemini"
          },
          "provider_selectors": {
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1
            },
            "description": "Environment variables that move this build onto a cloud provider -- Bedrock, Vertex, Foundry, GCA. **Never treat one as a credential.** An operator engaging one has changed something `model_api` does not claim to cover. Empty is a truthful answer for a build whose agent has no such switches.",
            "const": [
              "GOOGLE_GENAI_USE_VERTEXAI",
              "GOOGLE_GENAI_USE_GCA"
            ]
          },
          "auth_enforced": {
            "type": "boolean",
            "description": "Whether this BINARY checks the bearer token at all. Distinct from `auth_required` on `/healthz` and `/v1/deployment`, which means *a token is configured on this running instance* -- and a caller provisioning a container has no service to ask. An image that took a token and enforced nothing would report `auth_required: true` while protecting nothing.",
            "const": true
          },
          "endpoint_source": {
            "type": "string",
            "minLength": 1,
            "description": "The one environment variable that redirects this image's model traffic. **Singular, not a list**, because a consumer choosing from a list is a consumer guessing. Names a VARIABLE, never a URL, and is not derivable from `credential_sources`.",
            "const": "GOOGLE_GEMINI_BASE_URL"
          },
          "ca_bundle_source": {
            "type": [
              "object",
              "null"
            ],
            "additionalProperties": false,
            "required": [
              "variable",
              "shape",
              "replaces_default_trust"
            ],
            "properties": {
              "variable": {
                "type": "string",
                "minLength": 1,
                "description": "The environment variable an additional certificate authority is delivered under. A name, never a path: the path is the consumer's to choose.",
                "const": "NODE_EXTRA_CA_CERTS"
              },
              "shape": {
                "enum": [
                  "file",
                  "directory"
                ],
                "description": "Where to put the PEM. **Enumerated because the set really is closed** -- there is no third kind of filesystem entry -- unlike the per-build values around it, which are pinned with `const` instead.",
                "const": "file"
              },
              "replaces_default_trust": {
                "type": "boolean",
                "description": "Whether the default trust store SURVIVES. `true` means a container cannot reach a public host and a privately-signed one at the same time.",
                "const": false
              }
            },
            "description": "How a private certificate authority reaches this image, for a consumer behind its own TLS terminator. **An object rather than a name**, because a name alone cannot be acted on: a directory where a file is wanted fails exactly as a wrong name does. `null` is a real answer -- measured, and this build honours the OS trust store only -- and differs from the field being absent, which means nobody looked. A consumer guessing one name fleet-wide is wrong on at least one build."
          },
          "listen": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "address",
              "port"
            ],
            "properties": {
              "address": {
                "type": "string",
                "minLength": 1,
                "description": "The in-container bind address. `0.0.0.0` is IPv4 only.",
                "const": "0.0.0.0"
              },
              "port": {
                "type": "integer",
                "minimum": 1,
                "maximum": 65535,
                "description": "The in-container port to publish.",
                "const": 8000
              }
            },
            "description": "Where the service listens inside the container, so a port mapping can be decided before anything is started."
          },
          "runs_as": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "uid",
              "gid"
            ],
            "properties": {
              "uid": {
                "type": "integer",
                "minimum": 0,
                "description": "The numeric user id the service runs as.",
                "const": 1000
              },
              "gid": {
                "type": "integer",
                "minimum": 0,
                "description": "The numeric group id the service runs as.",
                "const": 1000
              }
            },
            "description": "**The NUMBERS, because a host directory needs a number.** A consumer that bind-mounts a directory it composes must chown it before the container exists, and Docker creates a missing mount point as `root:root 0755` -- so the mount is read-write and the first thing the agent writes fails, at a point where nothing names the cause. The image already answers `Config.User`, and the answer is the wrong type: a NAME, which means nothing on the host filesystem and resolves to a number only by running the container, which is strictly too late. **A mode is the consumer's to choose and an owner is not**, which is why this is published rather than left to a convention. Requested by Agent Harness (2026-08-19)."
          }
        }
      }
    }
  }
}
